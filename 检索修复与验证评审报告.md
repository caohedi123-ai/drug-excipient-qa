# 检索管线 · 第二轮修复 · 验证与评审报告

> 修复对象：`d:/药物原辅料知识问答助手/backend`
> 流程：我修复（覆盖审查报告 8 角度的高危/中危项）→ 子 agent 验证 → 子 agent 评审 → 端到端（mock 图）测试 → 交付
> 结论：**逻辑与流程层全部通过，可交付你在真实环境验收。**

---

## 一、本轮修复清单

| 编号 | 角度 | 修复内容 | 文件 |
|---|---|---|---|
| A | 边界 | 新增 `tools/sanitize.py`（输入清洗 + openFDA 转义）；API 入口校验 query 长度并清洗；fda_faers/fda_iig 短语转义、query 清洗 | sanitize.py / main.py / fda_faers.py / fda_iig.py |
| B | 失败判定/兜底 | FAILURE_MARKERS 扩展（覆盖"无结果/无匹配/API无返回"等）；轮次级 AnySearch 兜底在"无有效结果 OR 决策强制"时触发；兜底改为 `to_thread` 异步；统计成功/未找到/异常进 thinking_steps；未知工具跳过并记录 | retrieve.py |
| C | plan 约束 | `_validate_plan`：去重、剔除非注册工具、≤8 上限、优先非失效源 | plan.py |
| D | 终止/拒答 | graph 调用期传入 `recursion_limit`；decide 达上限设 `cannot_answer`（无证据或质量<0.35）；不足分支设 `force_fallback`；state 新增 failed_tools/force_fallback/cannot_answer/low_confidence；synthesize 在无法回答时加 ⚠️ 警示 | graph.py / decide.py / state.py / evaluate.py |
| E | 异常不吞 | fda_faers/fda_iig 的 `except: pass` 改为返回 `SearchResult.empty("...", f"API错误: {e}")`，原因可见 | fda_faers.py / fda_iig.py |
| F | 异步化 | 25 个源工具内同步引擎调用改为 `await asyncio.to_thread(...)`；引擎 async 包装函数同步改 to_thread；retrieve 改为真正 async，移除 `new_event_loop()` 嵌套 | 25 个 sources + anysearch_engine.py + tavily_engine.py + retrieve.py |
| G | 回归修复 | **（评审发现）** graph 编译期 `recursion_limit` 参数会让 `get_agent_graph()` 直接崩 → 改为调用期 config 传入；cannot_answer 阈值 0.5→0.35；入口截断 512→2000；fda_iig 匹配用清洗后 `q` | graph.py / decide.py / main.py / fda_iig.py |

---

## 二、验证结果（子 agent 验证 + 我的端到端测试）

### 2.1 子 agent 验证结论
验证 agent 逐一确认 6 项修复点（A–F）逻辑正确、可流转，**结论 PASS**。无法运行 python（无 shell），故以代码静态追踪为据。

### 2.2 单元自测 `test_retrieval_pipeline.py` —— 14 项全 PASS
- 30 工具注册、可调用、描述统一
- 失败工具 `success=False` 判定正确
- 轮次级 AnySearch 兜底触发且 `success=True`
- 成功场景保底不误触发
- AnySearch 引擎失败路径健壮（Citation 含 id）
- **新增**：`sanitize_query`/`escape_openfda_value`、`_validate_plan`（去重/校验/上限/失效源后置）、`decide` 无法回答出口 + 强制兜底标志、retrieve 跳过未知工具/failed_tools 累积/成败统计、`_finalize_plan` 强制兜底注入

### 2.3 真实图端到端冒烟 `test_graph_flow.py` —— 全 PASS（关键！）
用 mock 节点驱动**真实 LangGraph 状态机**（仅 decide 用真实逻辑），模拟"第1轮不足→调整→第2轮不足→调整→第3轮达上限强制合成"：

1. **旧 `recursion_limit=9` 确实抛 `GraphRecursionError`** —— 坐实评审指出的高危回归（测试绿但线上第 2 轮就崩、跑不满 3 轮）。
2. **修复后 `recursion_limit = max_rounds*6+10`：跑满 3 轮到达 synthesize，`cannot_answer=True`，无 GraphRecursionError**。
3. 真实 `decide` 路由：首轮失败+低质量 → `adjust_plan` 且 `force_fallback=True`。

> 这一步是"测试绿但端到端崩"陷阱的针对性证明——单元测试覆盖不到多轮回溯步数，必须用真实图跑满整轮。

### 2.4 模块导入/构建
- `get_agent_graph()` 编译成功（7 节点）—— 确认回退编译期 `recursion_limit` 后服务能正常构建。
- `import main` 成功。

---

## 三、评审结果与处理

评审 agent 独立审查改动，发现 **1 高危 + 4 中/低风险**，已全部处理：

| 评审项 | 严重度 | 处理 |
|---|---|---|
| #8 编译期 `recursion_limit` 会令 `get_agent_graph()` 直接 TypeError（图构建即崩） | **高** | 已改为**调用期** config 传入（main.py `config["recursion_limit"]`）；graph 编译不再传该参数；并补端到端测试证明旧值会崩、新值可跑满 |
| #1 `cannot_answer` 阈值 0.5 过严，有证据但质量分偏低会被误判"无法回答" | 中 | 阈值放宽为 0.35 |
| #6 入口 `sanitize_query` 截断到 512，长问（多药对比）丢实体 | 中 | 入口只清洗控制字符/零宽，保留实体（各源内部自行约束长度） |
| #5/#A `retrieve` 自建 `new_event_loop()` 有事件循环嵌套崩溃隐患 | 中 | `run_retrieve` 改为真正 async，graph 节点 `await`；移除 `new_event_loop`，`to_thread` 复用运行中的循环 |
| #3 `force_fallback` 在已有成功源时仍追加一次泛搜（轻微浪费） | 低 | 已知，暂不强制拦截（兜底多一次无害，优先保证"查得到"） |
| `fda_iig:62` 辅料匹配用原始 `query` 而非清洗后 `q` | 低 | 已改为 `q` |

评审总体结论：**改动可交付验收**（发现的回归均已修复）。

---

## 四、残余风险 / 需真实环境验收的项

1. **本沙箱离线**：无 LLM API、无搜索引擎外网、无 PostgreSQL/Redis，因此"真实联网问答"无法在此跑通。**请你在有网 + 已配 API Key + 已起 PG 的环境**启动服务做最终验收。
2. **前端引用字段契约不一致**（上一轮已发现，`ReferenceList` 取 `ref.source/title/score`，后端 citations 字段不同）——本轮未改前端，建议后续对齐。
3. **Tavily/AnySearch 付费额度治理**（跨轮缓存/预算/限流）未做，高并发下成本高。
4. **"换角度"检索策略**仍依赖 LLM 自由发挥，无确定性同义词/中英表（策略设计角度）。

---

## 五、如何启动与验收

```bash
cd d:/药物原辅料知识问答助手/backend
python main.py          # uvicorn，host/port 读 config
```

前端访问 `/api/chat`（SSE 流式）。验收要点：
- 提问一个**冷门/跨域**问题（如某小众辅料安全性），应在 thinking_steps 看到 `③ 兜底: 已自动启用 AnySearch 泛搜保底`，最终有来源答案而非"未找到"。
- 提问一个**专有源能答**的问题（如"阿司匹林机制"），不应触发兜底、直接给出带引用的答案。
- 故意问一个**完全不存在**的实体，应看到 `⑥ 路由决策: 已达最大轮次 → 强制合成 (cannot_answer=True)` 与答案前的 `⚠️` 警示，而非崩溃或无限转圈。
- 观察日志：每轮有 `③ 本轮: X 成功 / Y 未找到 / Z 异常` 统计，便于定位"这次为什么没查到"。

测试脚本可随时复跑：
```bash
cd d:/药物原辅料知识问答助手/backend
python test_retrieval_pipeline.py   # 单元/逻辑
python test_graph_flow.py           # 真实图多轮回溯端到端
```
