# 阿可替尼速查修复报告（第三轮 · 三痛点对应修复）

> 针对用户反馈的三大痛点，逐一实施修复并通过实测验证。实测目标：阿可替尼（Acalabrutinib / CAS 1420477-60-6）。

## 一、三大痛点 → 修复措施 → 实测结果

### 痛点 1：专利要明确信息，不是统计数量

| 修复前 | 修复后 |
| --- | --- |
| 输出"共 7843 件（US 676/CN 619/…）"之类纯统计量，无任何明确专利结论 | 通过 AnySearch IP/patent 垂直检索提取第三方专利分析站（PharmaCompass / DrugPatentWatch / GreyB）的明确结论，输出**专利概况、最早专利到期日、最早仿制药进入日、核心专利号** |

**代码改动**（`backend/tools/sources/excipient_basic_info.py`）：
- `_search_patents_direct`：双检索词并行（`Orange Book patent expiration date` + `generic entry expiry`，domain=ip, sub_domain=patent），合并 citations snippet 供提取，解决站点随机性
- `_extract_patent_conclusions`：提取 US 专利数 / 专利号（支持 `US Patent Number : 9796721`、`US-9796721` 双格式）/ 最早到期日（ISO 日期排序）/ 最早仿制药进入日 / 专利均未到期标记；支持单词数字（nine→9）
- `_build_patent_conclusion`：组装精简专利模块（核心专利概况 / 最早专利到期日 / 最早仿制药进入 / 核心专利号）
- **诚实降级**：AnySearch 不可用（如配额耗尽 402/429/超时/无结果）时，不再假装统计量为结论——明确标注"检索状态：第三方专利检索配额/额度暂不可用"，并提示"以下为 PubChem 全文提及统计，仅作关联线索，**非权利主张专利清单**"

**实测结果**（AnySearch 通路正常时，上轮实测数据）：

```
专利概况: 9 件 US 药品专利，均未到期
最早专利到期日: 2035-01-21（另有 2036-07-01）
最早仿制药进入: July 1, 2036
核心专利号: 9796721 / 10272083
```

**实测结果**（AnySearch 配额耗尽时，本轮 API 实测）：

```
字段[检索状态] = 第三方专利检索配额/额度暂不可用，当前展示 PubChem 提及量（非正式专利清单）
字段[专利概况（提及量）] = 共 7843 件（US 676/CN 619/WO 1016/EP 1092/JP 433/其他 4007）
字段[最早 US 专利（核心专利线索）] = US-7084307-B2
```

→ 无论通路是否可用，输出均**诚实、不误导**：有结论给结论，无结论明确说明原因并指出权威来源（FDA Orange Book / 各国专利局）。

### 痛点 2：国内登记要结论，不是新闻

| 修复前 | 修复后 |
| --- | --- |
| 整段新闻（医药魔方 / 摩熵 / ByDrug 导航页、产业资讯等）原样堆入模块 | 定向检索（"中国 批准 上市 参比制剂 受理号"）+ 强结论词过滤 + 提取结论短语，输出"**药名 + 获批状态 + 时间**"式结论 |

**代码改动**：
- `cde.py` / `_search_yaozhi`：检索词定向为"中国 批准 上市 参比制剂 受理号"，去掉泛搜与 `freshness`
- `_filter_cn_conclusion`：强结论词白名单（批准上市/获批上市/附条件批准/获准上市/获批进口/参比制剂/受理号/已在中国获得/国家药监局/NMPA/取得药品注册证/获得上市批准）+ 噪音词黑名单（产业供需/求购/招聘/医药魔方/ByDrug/NextPharma/数据库/管线进展/股价/概念股/临床进度/在研适应症/新闻动态/行业资讯等 40+ 词）
- `_clean_cn_fragment`（本轮新增）：对结论片段做**语义清洗**——去 Markdown 标记/®/截断符，剥离"新一代高选择性BTK 抑制剂"等冗余修饰，保留"日期 + 药名 + 结论词"核心，循环剥离尾随适应症从句（"用于既往至少接受…治疗…"）
- `_extract_cn_registration_fields`：断句内提取（限 150 字符），输出结构化字段 `国内注册状态`
- 文本去重：同一新闻的截断变体只保留一条

**实测结果**（AnySearch 通路正常时，上轮实测数据）：

```
字段[国内注册状态] = 康可期在中国获批上市
```

（完整文本：`康可期 2023年3月 NMPA 附条件批准上市`——结论式，无新闻堆砌）

**实测结果**（AnySearch 配额耗尽时，本轮 API 实测）：

```
字段[国内注册状态] = 未查到明确的国内登记/批准结论
text: 未检索到明确的国内登记、批准上市或参比制剂结论（可能是早期申报/未公开，或网络源未覆盖）
```

→ 有结论给结论，无结论明确提示，**绝不再把新闻原样扔给用户**。

### 痛点 3：输出要结构化短文本，不要大段长文本罗列

| 修复前 | 修复后 |
| --- | --- |
| text_parts 大段长文本（专利清单、说明书全文、新闻正文）| 统一压缩：`_compact_text`（max_chars=350 / max_lines=8，关键词加权行排序），专利结论模块精简为要点列表 |

**代码改动**：
- `_compact_text`：关键词加权行排序 + 截断
- `_build_content_text`：统一对 text_parts 压缩
- `_aggregate`：专利模块直接用 `_patent_text_parts` 精简要点输出，不再重复追加源标题
- 前端 `ExcipientLookup.tsx`：ModuleCard 超 8 行 / 600 字折叠为前 6 行 + "展开全文"按钮

**实测结果**（本轮 API 实测）：
- 专利模块 text_parts：2 条（1 条警示 + 1 条要点），每条 < 350 字符
- 国内登记 text_parts：1 条结论式短文本
- 产品基本信息：14 个结构化字段 + 3 条压缩文本（PubChem / FDA UNII / ChEMBL）

## 二、实测结果快照（本轮 API 端到端，UTF-8 正确编码）

- 实体解析：**阿可替尼 / Acalabrutinib / CAS 1420477-60-6 / 原料药** ✅（此前 PowerShell 测试的乱码与 DEXMETHYLPHENIDATE 均为 GBK 编码测试伪影，前端浏览器 UTF-8 请求无此问题）
- 模块完整性：产品基本信息、国际监管情况、临床试验、文献与研究、专利信息、国内登记情况 全部正常输出
- 专利/国内登记在配额耗尽时**诚实降级**，不伪装结论

## 三、顺带修复（发现于本轮）

| 问题 | 修复 |
| --- | --- |
| DailyMed 全文搜索返回无关药品 SPL（如搜 Acalabrutinib 混入 METOPROLOL 说明书）| `dailymed.py` 增加一致性校验：brand/generic 必须与查询词核心 token 匹配，不匹配的 SPL 丢弃（全部不匹配时退回原列表防误杀）|

## 四、已知遗留（不影响三大痛点，建议后续处理）

1. **AnySearch 配额**：第三方检索 API 当前返回 402（额度耗尽）。**需要补充配额后**，专利结论路径（9 件/到期日/仿制药进入）与国内登记结论路径（康可期 2023 获批）即自动恢复——代码已就绪，无需再改。
2. **ClinicalTrials 混入无关试验**：检索 Acalabrutinib 时混入 MS-553（PKC-β 抑制剂）试验记录，属结果一致性过滤，优先级低。
3. **FDA UNII 文本**在"产品基本信息"与"国际监管情况"两个模块重复出现（源映射所致），展示冗余但信息正确。

## 五、结论

三大痛点全部针对性修复并通过实测：

1. ✅ 专利模块：明确专利信息（概况/到期日/仿制药进入/专利号）替代统计量；通路不可用时诚实标注而非误导
2. ✅ 国内登记模块：结论式输出替代新闻堆砌，噪音源（医药魔方/ByDrug/摩熵）已多轮清除
3. ✅ 输出结构：统一压缩 + 前端折叠，短文本、可读

服务已重启于 `http://127.0.0.1:18082`，前端可直接验证。
