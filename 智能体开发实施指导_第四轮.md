# 智能体能力对比审查报告 - 实施指导（第四轮）

**文档类型**: 开发实施指南  
**目标读者**: 开发者智能体（用于开发药物原辅料知识问答助手）  
**文档用途**: 作为提示词直接输入给开发智能体，指导其实现健壮的智能体检索系统

---

## 一、背景与目标

### 1.1 背景

基于第三轮智能体能力对比审查报告，我们发现当前系统的7个维度存在差距：

| 维度 | 当前状态 | 目标状态 |
|------|---------|---------|
| 检索策略 | 一次性流水线 | 自适应迭代 |
| 兜底机制 | 2层浅层降级 | 4层深度降级链 |
| 结果验证 | 数量统计 | 内容校验 |
| 上下文保持 | 覆盖丢失 | 增量累积 |
| 工具选择 | LLM代理决策 | 规则+LLM混合决策 |
| 中英文处理 | 直接传递 | 自动翻译重试 |
| 元认知 | 不知道失败原因 | 能诊断失败原因 |

### 1.2 目标

开发一个**健壮的、自适应的、可维护的**药物原辅料知识问答智能体，具备以下能力：

1. **自适应迭代检索**：根据每步结果动态调整策略
2. **深度降级链**：工具失败时自动切换到备用方案
3. **内容质量校验**：检查检索内容是否真正回答问题
4. **上下文增量累积**：多轮检索时保留所有结果
5. **智能工具选择**：根据问题类型选择最合适的工具
6. **中英文自动切换**：一种语言失败自动换另一种
7. **元认知能力**：知道为什么失败，能引导用户

---

## 二、架构设计

### 2.1 核心架构：自适应迭代模式

**不要使用一次性流水线**，改用自适应迭代模式：

```
START → understand → plan → retrieve → validate_content → decide_next
                  ↑                                              │
                  └──────────── adjust_plan ←────────────────────┘
                                              │
                              内容充分 → synthesize → END
```

**关键节点说明：**

| 节点 | 职责 | 输入 | 输出 |
|------|------|------|------|
| understand | 实体识别、意图分类、子问题拆分 | user_query | entities, intent, sub_questions |
| plan | 选择工具、构造查询词 | sub_questions | tool_plan |
| retrieve | 调用工具、获取结果 | tool_plan | retrieval_results |
| validate_content | **检查内容质量**（新增） | retrieval_results, user_query | content_quality, missing_info |
| decide_next | **决定下一步**（新增） | content_quality, round_count | next_action |
| adjust_plan | **调整检索计划**（新增） | missing_info, current_plan | new_tool_plan |
| synthesize | 整合答案、标注引用 | retrieval_results, citations | final_answer |

### 2.2 状态定义

```python
class AgentState(TypedDict):
    # 基础信息
    user_query: str
    entities: list[str]
    intent: str
    sub_questions: list[str]
    
    # 检索相关
    tool_plan: list[dict]              # 当前检索计划
    retrieval_results: list[dict]      # 累积的检索结果（增量追加，不是覆盖）
    citations: list[dict]              # 累积的引用
    
    # 质量控制（新增）
    content_quality: float             # 内容质量评分（0-1）
    missing_info: list[str]            # 缺失的信息
    validation_details: dict           # 验证详情
    
    # 流程控制
    round_count: int                   # 当前轮次
    max_rounds: int                    # 最大轮次（默认5）
    next_action: str                   # "synthesize" | "adjust_plan" | "fallback"
    
    # 元认知（新增）
    failure_reasons: list[str]         # 失败原因列表
    suggestions: list[str]             # 给用户的建议
    
    # 输出
    final_answer: str
    thinking_steps: list[str]
```

---

## 三、核心实现

### 3.1 validate_content节点：内容质量校验

**这是最关键的改进**。不要只看citation数量，要检查内容是否真正回答问题。

```python
def validate_content_node(state: AgentState) -> dict:
    """
    检查检索内容的质量，判断是否足够回答用户问题。
    
    检查维度：
    1. 内容长度：是否足够详细
    2. 关键词匹配：是否包含答案关键词
    3. 数据一致性：多个来源是否一致
    4. 时效性：是否是最新数据（如果问题涉及时效）
    5. 来源可信度：是否来自权威来源
    """
    user_query = state.get("user_query", "")
    results = state.get("retrieval_results", [])
    intent = state.get("intent", "general")
    
    # 1. 提取问题关键词
    keywords = extract_keywords(user_query, intent)
    # 例如："阿司匹林的用法用量" → ["用法", "用量", "剂量", "dosage", "dose"]
    
    # 2. 检查内容长度
    total_length = sum(len(r.get("content", "")) for r in results)
    length_score = min(total_length / 2000, 1.0)  # 2000字为满分
    
    # 3. 检查关键词匹配
    content_text = " ".join(r.get("content", "") for r in results)
    match_count = sum(1 for kw in keywords if kw.lower() in content_text.lower())
    keyword_score = match_count / len(keywords) if keywords else 0
    
    # 4. 检查来源数量
    source_count = len(set(r.get("source_name", "") for r in results))
    source_score = min(source_count / 3, 1.0)  # 3个来源为满分
    
    # 5. 综合评分
    content_quality = (length_score * 0.3 + keyword_score * 0.5 + source_score * 0.2)
    
    # 6. 识别缺失信息
    missing_info = []
    if keyword_score < 0.5:
        missing_info.append("核心关键词未命中")
    if length_score < 0.3:
        missing_info.append("内容过于简短")
    if source_count < 2:
        missing_info.append("来源单一")
    
    # 7. 判断是否充分
    is_sufficient = content_quality >= 0.6 and not missing_info
    
    return {
        "content_quality": content_quality,
        "missing_info": missing_info,
        "validation_details": {
            "length_score": length_score,
            "keyword_score": keyword_score,
            "source_score": source_score,
            "total_length": total_length,
            "matched_keywords": [kw for kw in keywords if kw.lower() in content_text.lower()],
        },
        "next_action": "synthesize" if is_sufficient else "adjust_plan",
    }


def extract_keywords(query: str, intent: str) -> list[str]:
    """
    根据问题和意图提取关键词。
    
    策略：
    1. 分词 + 去停用词
    2. 根据意图添加领域关键词
    3. 中英文混合
    """
    import jieba
    import jieba.analyse
    
    # 中文分词
    zh_keywords = jieba.analyse.extract_tags(query, topK=5)
    
    # 根据意图添加领域关键词
    intent_keywords = {
        "dosage": ["用法", "用量", "剂量", "dosage", "dose", "administration"],
        "mechanism": ["机制", "作用", "靶点", "mechanism", "target", "pathway"],
        "safety": ["副作用", "不良反应", "安全性", "side effect", "adverse", "safety"],
        "registration": ["注册", "审批", "批准", "registration", "approval", "NDA"],
    }
    
    domain_keywords = intent_keywords.get(intent, [])
    
    # 合并去重
    all_keywords = list(set(zh_keywords + domain_keywords))
    
    return all_keywords
```

### 3.2 decide_next节点：智能决策

```python
def decide_next_node(state: AgentState) -> dict:
    """
    根据内容质量和轮次，决定下一步行动。
    
    决策逻辑：
    1. 内容充分 → synthesize
    2. 内容不足 + 未达最大轮次 → adjust_plan（调整策略重试）
    3. 内容不足 + 已达最大轮次 → fallback（降级回答）
    """
    content_quality = state.get("content_quality", 0)
    round_count = state.get("round_count", 0)
    max_rounds = state.get("max_rounds", 5)
    missing_info = state.get("missing_info", [])
    
    # 内容充分
    if content_quality >= 0.6:
        return {"next_action": "synthesize"}
    
    # 内容不足 + 未达最大轮次
    if round_count < max_rounds:
        return {"next_action": "adjust_plan"}
    
    # 内容不足 + 已达最大轮次 → 降级回答
    failure_reasons = []
    if "核心关键词未命中" in missing_info:
        failure_reasons.append("未找到直接相关内容")
    if "内容过于简短" in missing_info:
        failure_reasons.append("检索到的信息不够详细")
    if "来源单一" in missing_info:
        failure_reasons.append("数据来源单一，无法交叉验证")
    
    suggestions = [
        "您可以尝试使用药品英文名查询",
        "您可以尝试查询更具体的问题",
        "您可以访问 https://dailymed.nlm.nih.gov 获取更多信息",
    ]
    
    return {
        "next_action": "fallback",
        "failure_reasons": failure_reasons,
        "suggestions": suggestions,
    }
```

### 3.3 adjust_plan节点：动态调整策略

```python
def adjust_plan_node(state: AgentState) -> dict:
    """
    根据缺失信息，调整检索计划。
    
    调整策略：
    1. 关键词未命中 → 换关键词（同义词、中英文切换）
    2. 内容过于简短 → 换工具（从API切换到搜索引擎抓取全文）
    3. 来源单一 → 增加数据源
    """
    missing_info = state.get("missing_info", [])
    current_plan = state.get("tool_plan", [])
    round_count = state.get("round_count", 0)
    
    new_plan = []
    
    # 策略1：关键词未命中 → 换关键词
    if "核心关键词未命中" in missing_info:
        # 使用同义词或翻译
        for item in current_plan:
            tool = item.get("tool")
            query_en = item.get("query_en", "")
            query_zh = item.get("query_zh", "")
            
            # 中英文切换
            if round_count % 2 == 0:
                new_query = translate_to_english(query_zh) if query_zh else query_en
            else:
                new_query = translate_to_chinese(query_en) if query_en else query_zh
            
            new_plan.append({
                "tool": tool,
                "query_en": new_query,
                "query_zh": query_zh,
                "reason": "关键词调整",
            })
    
    # 策略2：内容过于简短 → 换工具（从API切换到搜索引擎）
    if "内容过于简短" in missing_info:
        # 优先使用Tavily抓取全文
        new_plan.append({
            "tool": "tavily_search",
            "query_en": state.get("user_query", ""),
            "query_zh": state.get("user_query", ""),
            "reason": "切换到搜索引擎抓取全文",
        })
    
    # 策略3：来源单一 → 增加数据源
    if "来源单一" in missing_info:
        # 根据意图选择额外的数据源
        intent = state.get("intent", "general")
        additional_tools = {
            "dosage": ["dailymed_tool", "fda_drugs_tool"],
            "mechanism": ["pubchem_tool", "drugbank_tool"],
            "safety": ["fda_faers_tool", "pubmed_tool"],
        }
        
        for tool in additional_tools.get(intent, []):
            if tool not in [p.get("tool") for p in current_plan]:
                new_plan.append({
                    "tool": tool,
                    "query_en": state.get("user_query", ""),
                    "query_zh": state.get("user_query", ""),
                    "reason": "增加数据源",
                })
    
    return {
        "tool_plan": new_plan,
        "round_count": round_count + 1,
    }
```

### 3.4 retrieve节点：增量累积

**关键修改：retrieval_results改为追加，不是覆盖**

```python
def retrieve_node(state: AgentState) -> dict:
    """调用工具，增量累积结果"""
    tool_plan = state.get("tool_plan", [])
    existing_results = state.get("retrieval_results", [])  # 已有的结果
    existing_citations = state.get("citations", [])
    
    new_results = []
    new_citations = []
    
    for item in tool_plan:
        tool_name = item.get("tool")
        query = item.get("query_en", item.get("query_zh", ""))
        
        # 调用工具
        result = invoke_tool(tool_name, query)
        
        if result.get("success"):
            new_results.append(result)
            new_citations.extend(result.get("citations", []))
    
    # 关键：增量追加，不是覆盖
    all_results = existing_results + new_results
    all_citations = existing_citations + new_citations
    
    return {
        "retrieval_results": all_results,  # 追加
        "citations": all_citations,        # 追加
    }
```

### 3.5 synthesize节点：元认知

```python
def synthesize_node(state: AgentState) -> dict:
    """整合答案，如果失败则给出诊断"""
    next_action = state.get("next_action", "synthesize")
    
    # 如果是降级回答
    if next_action == "fallback":
        failure_reasons = state.get("failure_reasons", [])
        suggestions = state.get("suggestions", [])
        
        answer = "抱歉，" + "，".join(failure_reasons) + "。\n\n"
        answer += "建议：\n"
        for i, suggestion in enumerate(suggestions, 1):
            answer += f"{i}. {suggestion}\n"
        
        return {"final_answer": answer}
    
    # 正常合成
    results = state.get("retrieval_results", [])
    citations = state.get("citations", [])
    user_query = state.get("user_query", "")
    
    # 调用LLM整合
    answer = llm_synthesize(results, citations, user_query)
    
    return {"final_answer": answer}
```

---

## 四、工具设计：4层深度降级链

### 4.1 当前工具的核心问题：只查目录，不取内容

**问题本质**：工具只做了"查目录"（调用API获取元数据），没做"取内容"（抓取实际答案）。

**以DailyMed为例**：

```python
# 当前实现（有问题）
async def dailymed_tool(query: str) -> dict:
    # 第1步：查目录
    resp = await client.get(
        f"{DAILYMED_URL}/spls.json",
        params={"search_text": query, "limit": 5}
    )
    
    # 第2步：只提取元数据（品牌名、生产商）
    for spl in spls[:5]:
        content_parts.append(
            f"[{i}] {brand}\n"
            f"    通用名: {generic}\n"
            f"    生产商: {manufacturer}\n"
            f"    URL: {label_url}"
        )
    
    # 问题：没有抓取标签全文，没有提取"用法用量"章节
    return SearchResult(content="\n".join(content_parts))
```

**返回结果**：
```
[1] Aspirin
    通用名: aspirin
    生产商: Bayer
    URL: https://dailymed.nlm.nih.gov/dailymed/drugInfo.cfm?setid=xxx
```

**LLM看到什么**：
- "有这个药"
- "生产商是Bayer"
- "点这个URL可以看到更多信息"
- **但看不到"用法用量"的实际内容**

**正确的实现应该**：

```python
# 正确实现
async def dailymed_tool(query: str) -> dict:
    # 第1步：查目录
    spls = await search_dailymed(query)
    
    # 第2步：取内容（关键！）
    for spl in spls[:3]:
        # 抓取标签全文
        full_text = await fetch_full_label(spl["setid"])
        
        # 提取"用法用量"章节
        dosage_section = extract_section(full_text, "Dosage and Administration")
        
        # 返回实际内容
        content_parts.append(dosage_section)
    
    return SearchResult(content="\n".join(content_parts))
```

**比喻**：
- API是图书馆的图书管理系统
- 工具是图书管理员
- 内容是书本身

**当前问题**：
- 你问管理员："有没有《阿司匹林用法指南》？"
- 管理员查系统："有，在A区3排"
- 管理员告诉你："书在A区3排"
- **但不把书拿给你**
- 你看到"书在A区3排"的信息，但看不到书的内容

**正确做法**：
- 管理员查系统："有，在A区3排"
- **去A区3排把书拿给你**
- 你看到书的实际内容

### 4.2 其他工具的类似问题

| 工具 | 当前问题 | 应该做什么 |
|------|---------|-----------|
| DailyMed | 只返回标签元数据 | 抓取标签全文，提取相关章节 |
| DrugBank | Tavily返回摘要 | 抓取DrugBank页面全文 |
| PubChem | 只返回化学性质 | 查PubChem的"Pharmacology"章节 |
| FDA Drugs | 只返回审批元数据 | 抓取FDA标签全文 |
| PubMed | 只返回摘要 | 抓取PubMed全文（如果有开放获取） |

### 4.3 降级链设计

每个工具都应该有4层降级：

```
第1层：官方API（结构化查询）
  ↓ 失败或内容不够
第2层：Tavily域名定向搜索（抓取页面摘要）
  ↓ 失败或内容不够
第3层：AnySearch垂直领域搜索（不限域名）
  ↓ 失败或内容不够
第4层：Tavily抓取全文（include_raw_content=True）
  ↓ 失败或内容不够
第5层：返回空结果 + 建议（引导用户去权威网站查询）
```

**说明**：
- 不使用浏览器自动化（Browser automation），因为部署环境复杂且不稳定
- 不使用Exa搜索引擎，因为是付费服务，成本较高
- 最终降级为"诚实告知+引导用户"，而不是强行编造答案

### 4.2 工具实现示例

```python
async def dailymed_tool(query: str) -> dict:
    """
    DailyMed工具：4层降级链
    
    第1层：DailyMed REST API
    第2层：Tavily域名定向搜索
    第3层：AnySearch health垂直搜索
    第4层：Tavily抓取全文
    第5层：返回空结果 + 建议
    """
    
    # 第1层：DailyMed API
    try:
        api_result = await call_dailymed_api(query)
        if api_result.get("success") and len(api_result.get("content", "")) > 500:
            return api_result
    except Exception as e:
        log_error(f"DailyMed API失败: {e}")
    
    # 第2层：Tavily域名定向搜索
    try:
        tavily_result = tavily_domain_search(
            query=query,
            domains=["dailymed.nlm.nih.gov"],
            search_depth="advanced",
            include_answer=True,
        )
        if tavily_result.get("success") and len(tavily_result.get("content", "")) > 500:
            return tavily_result
    except Exception as e:
        log_error(f"Tavily搜索失败: {e}")
    
    # 第3层：AnySearch health垂直搜索
    try:
        anysearch_result = anysearch_vertical(
            query=query,
            domain="health",
            freshness="past_year",
        )
        if anysearch_result.get("success") and len(anysearch_result.get("content", "")) > 500:
            return anysearch_result
    except Exception as e:
        log_error(f"AnySearch搜索失败: {e}")
    
    # 第4层：Tavily抓取全文
    try:
        tavily_full = tavily_domain_search(
            query=query + " dosage administration",
            domains=["dailymed.nlm.nih.gov"],
            search_depth="advanced",
            include_raw_content=True,  # 抓取全文
        )
        if tavily_full.get("success"):
            return tavily_full
    except Exception as e:
        log_error(f"Tavily全文抓取失败: {e}")
    
    # 第5层：返回空结果 + 建议
    return {
        "success": False,
        "content": "",
        "citations": [],
        "suggestion": "建议直接访问 https://dailymed.nlm.nih.gov 查询",
    }
```

---

## 五、验收标准

### 5.1 功能验收

| 测试用例 | 预期结果 | 验收标准 |
|---------|---------|---------|
| "阿司匹林的用法用量" | 返回DailyMed的用法用量内容 | 内容长度>500字，包含"dosage"关键词 |
| "阿司匹林的分子量" | 返回PubChem的分子量 | 数值准确（180.16 g/mol） |
| "阿司匹林和布洛芬的区别" | 返回多源对比信息 | 至少3个数据源命中 |
| "阿司匹林的最新临床试验" | 返回临床试验信息 | 包含ClinicalTrials.gov的引用 |
| "不存在的药品XYZ" | 诚实告知未找到 | 给出建议，不编造 |

### 5.2 性能验收

| 指标 | 目标值 |
|------|-------|
| 平均响应时间 | <30秒 |
| 成功率 | >80% |
| 内容质量评分 | >0.6 |
| 引用准确率 | >90% |

### 5.3 健壮性验收

| 测试场景 | 预期行为 |
|---------|---------|
| API超时 | 自动降级到搜索引擎 |
| 所有工具失败 | 诚实告知，给出建议 |
| 中文query | 自动翻译为英文重试 |
| 多轮检索 | 增量累积结果，不丢失 |
| 内容不充分 | 自动调整策略重试 |

---

## 六、部署清单

### 6.1 环境要求

```yaml
Python: 3.12+
Node.js: 20+
PostgreSQL: 16
Redis: 7
```

### 6.2 依赖安装

```bash
# Python依赖
pip install langgraph langchain langchain-openai tavily-python httpx pydantic-settings

# Node.js依赖（前端）
npm install react react-dom vite
```

### 6.3 配置文件

```yaml
# .env
DEEPSEEK_API_KEY=***
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat

TAVILY_API_KEY=***
ANYSEARCH_API_KEY=***

DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/pharma_kb
REDIS_URL=redis://localhost:6379/0
```

---

## 七、开发路线图

### Phase 1：核心架构（3天）

- [ ] 实现AgentState定义
- [ ] 实现understand节点
- [ ] 实现plan节点
- [ ] 实现retrieve节点（增量累积）
- [ ] 实现validate_content节点
- [ ] 实现decide_next节点
- [ ] 实现adjust_plan节点
- [ ] 实现synthesize节点

### Phase 2：工具实现（3天）

- [ ] 实现12个数据源工具（每个4层降级）
- [ ] 实现Tavily搜索引擎封装
- [ ] 实现AnySearch搜索引擎封装

### Phase 3：前端UI（2天）

- [ ] 部署开源Chat UI模板
- [ ] 对接后端API
- [ ] 实现引用展示
- [ ] 实现思考过程展示

### Phase 4：测试与优化（2天）

- [ ] 功能测试（5个核心用例）
- [ ] 性能测试（响应时间、成功率）
- [ ] 健壮性测试（超时、失败、多轮）
- [ ] 优化prompt和策略

### Phase 5：部署上线（1天）

- [ ] 部署到腾讯云服务器
- [ ] 配置Nginx反向代理
- [ ] 配置SSL证书
- [ ] 配置监控和告警

**总计：11天**

---

## 八、常见问题

### Q1：为什么不用LangGraph的内置状态管理？

**A**：LangGraph的内置状态管理是"覆盖"模式，不是"追加"模式。我们需要增量累积retrieval_results，所以自己管理状态。

### Q2：为什么validate_content不用LLM判断？

**A**：LLM判断慢（1-3秒）且不稳定。用规则判断（关键词匹配、长度检查）更快更可靠。只有复杂场景才用LLM辅助。

### Q3：为什么工具要有4层降级？

**A**：因为单一工具不可靠。API可能超时，搜索可能没结果，页面可能被拦截。4层降级保证"总有一层能成功"。不使用浏览器自动化是因为部署环境复杂且不稳定；不使用Exa是因为它是付费服务，成本较高。

### Q4：为什么中文query要自动翻译？

**A**：因为大部分权威数据源（PubChem、DailyMed、FDA）只支持英文。中文query直接搜会失败。

### Q5：为什么要有元认知能力？

**A**：因为用户需要知道"为什么没找到"。如果只说"未找到"，用户不知道是工具问题、query问题还是数据源问题。元认知能诊断失败原因，给出针对性建议。

---

## 九、总结

这份实施指导的核心思想是：

1. **自适应迭代**：不要一次性流水线，要根据每步结果动态调整
2. **深度降级**：每个工具都要有4层降级链
3. **内容校验**：不要只看citation数量，要检查内容质量
4. **增量累积**：多轮检索时保留所有结果
5. **元认知**：知道为什么失败，能引导用户

**按照这份指导开发，你的智能体会比当前的问答助手健壮得多。**

---

**文档结束**

**提示词使用方式**：将本文档完整输入给开发智能体，让它按照指导实现药物原辅料知识问答助手。
