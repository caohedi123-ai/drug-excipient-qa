# 线上故障修复报告

## 一、故障现象
部署完成后，网站两个核心功能无法使用：
1. **问答板块**：点发送问题发不出去（请求报错，无结果返回）
2. **速查板块**：一点击速查就出错（返回 503「速查工具未加载」）

## 二、根因（通俗版）
| 功能 | 原因 | 打比方 |
|---|---|---|
| 速查 503 | 服务器上少了 3 个「登记文件」（Python 包声明文件），系统找不到速查工具 | 快递员不知道包裹该放哪个柜子，直接拒收 |
| 问答发不出去 | 后台用了「异步收发通道」对接数据库，但服务器上装的组件只支持「同步」，一对接就报错 | 传真机只支持传真，你却让它发微信，当然失败 |

## 三、修复动作
1. 补齐服务器缺失的包声明文件（`tools` 及其子目录共 3 个 `__init__.py`），并把本地全部修复代码同步到服务器
2. 将问答后台的数据库记忆组件从「同步版」换成「异步版」（`AsyncPostgresSaver`），并在启动时自动检测、自动降级
3. 重新构建后端镜像并重启容器

## 四、线上验证结果（已通过）
- 系统健康检查：✅ 通过，数据库记忆组件已正常连接（不再降级）
- 登录：✅ 正常
- **速查**：✅ 查询「阿司匹林」正常返回产品信息（CAS 号 50-78-2、PubChem 编号 2244、产品类型等）
- **问答**：✅ 提问「阿司匹林是什么药？」完整返回——含思考过程、化学信息（C9H8O4）、临床用途、不良反应与安全警告、剂型、ATC 分类、RxNorm 编号等，并带引用标注

## 五、建议
请直接打开网站，实际点一次「速查」和「问答」，确认两个功能都能正常使用。
