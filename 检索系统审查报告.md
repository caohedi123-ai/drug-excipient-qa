# 检索问答系统 · 八角度白盒审查问题清单

> 审查对象：`d:/药物原辅料知识问答助手/`（后端 `backend/` + 前端 `frontend/`）
> 审查方式：8 个独立子 agent 分别从【白盒测试 / 流程阻塞 / 边界处理 / 异常处理 / 黑盒测试 / 代码设计 / 策略设计 / 架构设计】静态深挖真实代码，未改动任何代码。
> 说明：每条问题均附 `文件:行号` 证据；严重程度 高/中/低。本报告仅列问题，不含修复。

---

## 总览：最优先修复的 12 项（高严重度）

| # | 角度 | 问题 | 文件 |
|---|---|---|---|
| P1 | 架构/异常 | 搜索引擎引擎用**同步** httpx/Tavily，在 LangGraph 异步事件循环内阻塞，并行退化为串行 | anysearch_engine.py:59、tavily_engine.py:53、retrieve.py:54 |
| P2 | 策略 | 轮次级 AnySearch 兜底仅"全部无效"才触发，"假成功"源会让兜底**永不触发** | retrieve.py:109-111 |
| P3 | 边界 | API 入口对 `query` **无长度/空值/内容校验**，噪声/超长 query 直透后端 | main.py:41-44 |
| P4 | 边界 | openFDA 查询语法内 `query` 直接拼入双引号，**未转义→注入/400/错结果** | fda_faers.py:37,58；fda_iig.py:40 |
| P5 | 异常 | 30 个源工具大量 `except: pass` **静默吞异常**，掩盖 key 缺失/限流/网络错误 | 28+ 个 source 文件 |
| P6 | 策略 | plan 把工具选择**全权交 LLM**，无数量上限/去重/失效源记忆，可能一轮 30 并发付费调用 | plan.py:36,92 |
| P7 | 策略 | 达 `max_rounds` 时**强制 `is_sufficient=True` 合成**，无"无法回答"出口 | decide.py:30-39 |
| P8 | 策略/异常 | 决策级兜底仅是**文本建议**喂 LLM，不保证 `anysearch_fallback` 真被选中 | decide.py:60-62→adjust.py:50-56 |
| P9 | 流程 | 图编译未设 `recursion_limit`，依赖 `decide` 软逻辑防死循环，脱钩时有 `GraphRecursionError` 硬崩 | graph.py:124-135 |
| P10 | 边界 | `SearchResult.empty()` 的 `[无结果]` **被误判为 success=True**（`FAILURE_MARKERS` 漏词） | state.py:47-53；retrieve.py:89-99 |
| P11 | 代码设计 | 30 个源工具**复制粘贴**同一套 `[Name] content + __citations__` 字符串协议，改一处必崩另一处 | 全部 source 文件 + retrieve.py:70-87 |
| P12 | 架构 | DESIGN.md 宣称"ChEMBL MCP / AnySearch MCP"**实际未实现**，全为普通 REST，文档误导 | DESIGN.md:404 vs chembl.py:13 |

---

## 一、白盒测试角度（测试覆盖与内部逻辑）

1. **【高】测试仅覆盖 `retrieve` 单节点**，understand/plan/evaluate/decide/adjust/synthesize 六节点零覆盖；未跑端到端 `build_graph().compile().invoke()`。— `test_retrieval_pipeline.py`、`graph.py:74-110`
2. **【高】`retrieve` 内部分支未覆盖**：空 plan 默认分支、工具抛异常分支、畸形 `__citations__:` JSON、`[无结果]` 保底分支（仅覆盖"触发成功保底"）。— `retrieve.py:39-41,54-68,80-81,110-131`
3. **【中】`_invoke_tool` 4 个兼容分支仅 1 条可达**：30 个工具都是 `@tool async def`，`.func` 恒为协程，分支 2/3/4 永不可达（死代码）。— `retrieve.py:162-184`
4. **【高】`decide` 4 条决策路径未覆盖**：最大轮次强制合成、充分+high/medium、充分+low 且质量≥0.5、不足→adjust。— `decide.py:30-75`
5. **【中】`evaluate`/`validate` 降级路径未覆盖**：LLM 异常降级、无检索结果分支、低分缺失项分支。— `evaluate.py:144-158`、`validate.py:20-26`
6. **【高】`adjust` 读取 `failure_reasons` 后未使用（`feedback` 变量 unused，数据死区）**。— `adjust.py:41`
7. **【中】`confidence` 字段数据死区**：evaluate 写入但下游 synthesize 不接收、不展示。— `evaluate.py:122,140`、`synthesize.py:209-265`
8. **【中】状态字段读写不对称**：部分字段写入从未读、读从未写（需全量字段审计 `state.py`）。
9. **【低】prompt 拼接 / citation 重编号逻辑缺边界单测**。

---

## 二、流程阻塞角度（死循环 / 卡死）

1. **【高】图编译未设 `recursion_limit`**，默认 25，与业务 `max_retrieval_rounds=3` 脱钩；`decide` 软逻辑是唯一终止保险，失灵即 `GraphRecursionError` 硬崩。— `graph.py:124-135`
2. **【中高】`round_count` 累加依赖 `retrieve` 每轮执行**，若被重试/重放破坏则可能突破 3 轮上限。— `retrieve.py:32,156`；`decide.py:30`
3. **【中】"假成功"提前终止**：`decide` 只看 LLM `is_sufficient`，即使全部 `success=False` 也直接 synthesize 出空答案。— `decide.py:42-53`
4. **【中】state 累积无截断**：`retrieval_results` 每轮追加，多轮膨胀拖慢/撑爆上下文。— `retrieve.py:133-159`
5. **【中】LLM 调用无超时/重试**：`ChatOpenAI` 未设 `request_timeout/max_retries`，网络挂起可长时间阻塞。— 全 LLM 调用点
6. **【中】全局无 try**：任一节点未捕获异常 → graph 整体失败无响应。— 各 node
7. **【低】前端无自动超时**：`fetch` 仅 `AbortController` 手动停止，无 `setTimeout`/`AbortSignal.timeout()`，可能一直 loading。— `frontend/src/lib/api.ts:70`

---

## 三、边界处理角度（极端输入）

1. **【高】API 入口无 query 校验**（长度/空值/特殊字符）。— `main.py:41-44,166`
2. **【高】openFDA 查询 `query` 未转义**（双引号/`:`/空格破坏 Lucene 语法）。— `fda_faers.py:37,58`；`fda_iig.py:40`
3. **【中】PubChem/Wikipedia URL 路径未编码**（中文/`#`/`?`/`%` 截断或错查）。— `pubchem.py:41,52`；`wikipedia.py:33,47`
4. **【中】多工具 query 未清洗**（换行/控制字符/emoji/超长进 API）。— chembl/clinicaltrials/pubmed/dailymed/cde/cnipa 等
5. **【中】citation id 重编号依赖 `__citations__:` 分支**，非此路径带 citations 时不重编号可能冲突。— `retrieve.py:84-87,116-119`
6. **【中】`SearchResult.empty()` 的 `[无结果]` 被误判 success=True**（`FAILURE_MARKERS` 缺 `"无结果"`/`"[无结果]"`）。— `state.py:47-53`；`retrieve.py:89-99`
7. **【中】plan 选不存在工具名时静默跳过**无提示（漏检/兜底缺位）。— `retrieve.py:47-53`
8. **【低】降级 plan 用原始 query**（中文长句当英文名喂 PubChem）。— `plan.py:115`；`adjust.py:87`
9. **【低】合成阶段各源独立截断但不限总 token**，30 源可达 60k+ 字符撑爆上下文。— `synthesize.py:227-230`
10. **【低】markdown `split("```")[1]` 无闭合 ``` 时 IndexError** 静默降级。— understand/plan/evaluate/adjust 各处
11. **【低】达 max_rounds 强制 is_sufficient=True** 静默降质。— `decide.py:30-39`
12. **【低】config 0/负数无校验**：`max_retrieval_rounds=0` 首轮即合成、`max_results=0` 全空。— `config.py:35`
13. **【中】前端零校验透传**。— `frontend/src/lib/api.ts:61-83`
14. **【低】工具异常转字符串污染下游 prompt**（可能含内部 URL/凭证痕迹）。— `retrieve.py:183-184`
15. **【低】emoji/零宽字符使关键词匹配失效**（NFKC 未归一）。— `validate.py:32`；`understand.py:108`

---

## 四、异常处理角度（错误被吞 / 误判）

1. **【高】28+ 源工具 `except: pass` 静默吞异常**，掩盖 key 缺失/限流/网络，与"真无数据"无法区分。— dailymed/clinicaltrials/wikipedia/fda_*/chembl/ema/cde/coconut/open_targets/rxnorm/drugcentral/pubchem 等
2. **【中】引擎 HTTP 无重试/无 429·5xx 退避**：AnySearch/Tavily/Exa 均单次调用，限流持续触发浪费配额。— anysearch_engine.py:59-65；tavily_engine.py:103-112；exa_engine.py:67-74
3. **【高】`FAILURE_MARKERS` 两类风险并存**：① 合法内容含 "not found"/"未找到" 被误判失败；② 引擎层 `"API无返回"`/`"搜索无结果"`/`"无匹配文献"` 等占位词**不在列表**，被误判成功。— `retrieve.py:91-99`；各引擎 reason 文案
4. **【中】`SearchResult`/`Citation` 构造缺字段即 TypeError**，无统一工厂防呆（此前已修 source_name/id，但缺 `error` 结构化字段）。— `state.py`
5. **【中】LLM 调用异常无 try/except 兜底**：plan/decide/evaluate/synthesize 崩溃即整图失败。— 各 node
6. **【中】main.py 缺全局异常捕获**，错误可能以 500 堆栈而非友好提示返回。— `main.py`
7. **【低】httpx 连接未复用/未关闭**，异常路径可能泄漏连接。— 各引擎
8. **【低】异常字符串进入 prompt** 可能泄露内部 URL/凭证（见边界 14）。

---

## 五、黑盒测试角度（用户可观察行为）

1. **【高】全部非药物专业源失败均静默降级 Tavily**，失败对用户不可见，误导"权威源给出"。— `pubchem.py:58-60`、`tavily_engine.py:103-112`、`thinking_steps` 不区分主源/兜底
2. **【高】全不可用仍会"合成"答案**，可能 LLM 幻觉或返回"无法回答"。— `decide.py:30-39`、`synthesize.py:269-282`
3. **【中】错误拼写/口语化被"伪命中"**，无澄清提示。— `retrieve.py:91-99`、`pubchem.py:48-56`
4. **【中】中文查询对英文源（PubMed/FDA）几乎必然空转**。— `understand.py:42`、`plan.py:50-53`
5. **【高】前端引用字段契约与后端不一致**：后端 `citations` 含 `retrieval_query/retrieval_timestamp`，前端 `ReferenceList` 用 `ref.source/ref.title/ref.subtitle/ref.score` 取不到。— `main.py:228-235`、`state.py:23-31`、`frontend/components/ReferenceList.tsx`
6. **【中】无结果时 citations 可能为空/格式错**，前端无降级展示。— `synthesize.py` + 前端
7. **【中】同一问题多次问结果差异大**（LLM 非确定性），无缓存/去重。
8. **【中】答案质量风险**：synthesize 可能编造无引用内容，citations 与正文对应弱。
9. **【低】性能黑盒**：端到端耗时受 LLM 轮次 + 工具并发 + 网络主导，无瓶颈治理。

---

## 六、代码设计角度（代码质量 / DRY）

1. **【高】`[Name] content + __citations__:` 模板在 30 个源复制粘贴**，隐式强耦合字符串协议，改一处必崩。— 全部 source + `retrieve.py:70-87`
2. **【高】`anysearch_vertical(query+"后缀")` 在 16+ 源逐字重复**，应抽 `anysearch_source(name,query,suffix)` 工厂。— bindingdb/ddinter/ttd/who_atc/cde/ich/sider/pharmgkb 等
3. **【中】引擎内 Citation 构造循环重复** 3+ 处。— anysearch/tavily/exa 引擎
4. **【中】节点经共享可变 state 字典强耦合**，关键私有 `_plan` 跨模块隐式约定且命名误导。— `state.py:72`、`plan.py:108`、`retrieve.py:28`、`adjust.py:81`
5. **【低】graph.py 薄 wrapper 节点**仅做延迟 import+转发，且模块级 `settings=get_settings()` import 时触发。— `graph.py:15-56`
6. **【中】`FAILURE_MARKERS` 内联硬编码**且 `retrieve.py`/`anysearch_engine.py`/`anysearch_fallback.py` 用不一致字面量（"No results" vs "No results found."）。— `retrieve.py:91-94,114`
7. **【中】工具名硬编码散落**于 prompts/nodes/tests。— `plan.py:38-54`
8. **【中】降级默认 plan 写死 `pubchem+wikipedia`**，与问题领域无关。— `adjust.py:86-89`
9. **【低】`_invoke_tool` 死代码分支**（见白盒 3）。
10. **【低】命名/注释/死导入**零散。

---

## 七、策略设计角度（检索与兜底策略）

1. **【高】plan 无工具数量上限/去重/失效源记忆**，可能一轮 30 并发付费调用且反复选同一失效源。— `plan.py:36,92`；`adjust.py:37-83`；`state.py:75`
2. **【高】轮次级兜底因"假成功"不触发**（见边界 6 / 异常 3）。— `retrieve.py:109-111`
3. **【中】无成本/优先级分层调度**，免费 API 与付费 Tavily/AnySearch 同等对待，无 `Semaphore` 并发上限。— `retrieve.py:44-54`
4. **【中】决策级兜底仅文本注入**，不强制 `anysearch_fallback`。— `decide.py:60-62`
5. **【中】AnySearch 兜底仅用原始 `user_query`**，未结合缺失信息/子问题。— `retrieve.py:113`
6. **【高】"换角度"无确定实现**，纯靠 LLM 自由发挥（无同义词/中英表）。— `adjust.py:12-34,78`
7. **【中】`adjust` 降级 plan 写死 pubchem+wikipedia**。— `adjust.py:86-89`
8. **【中】`source_score` 3 源即满分 + 子串关键词匹配易误判**（"aspirin" 命中 "aspirin-resistant"，"机制" 命中一切）。— `validate.py:32,41-42`
9. **【中】evaluate 规则分≥0.6 即判充分（LLM 异常降级）**，易虚高。— `evaluate.py:144-151`
10. **【中】decide 安全网 `content_quality>=0.5` 阈值偏低**，置信度 low 仍强行合成。— `decide.py:46-51`
11. **【低】评估截断 800/3000 字符**可能误判关键内容不足。— `evaluate.py:86`、`retrieve.py:103`
12. **【高】达轮次上限强制合成，无"无法回答"出口 + 置信度不透出**。— `decide.py:29-39`、`evaluate.py:122`
13. **【高】Tavily 付费额度被 25/26 源各自兜底反复消耗**，无预算/缓存。— `pubchem.py:60,99` 等
14. **【中】无检索结果缓存/去重**，跨轮跨会话重复消耗 API。— `retrieve.py:133-159`、config 有 `redis_url` 未用

---

## 八、架构设计角度（分层 / 与设计偏差）

1. **【高】引擎同步 IO 阻塞事件循环**（见总览 P1）。— anysearch/tavily/exa 引擎 + `retrieve.py:54`
2. **【中】`retrieve` 每次 `asyncio.new_event_loop()`**，与 FastAPI 异步运行时割裂，丧失并发。— `retrieve.py:17-23`
3. **【高】DESIGN.md 宣称 MCP（ChEMBL/AnySearch）未实现**，全为普通 REST，文档误导。— `DESIGN.md:404` vs `chembl.py:13`
4. **【高】文档宣称"24 工具"实为 30，且 `anysearch_fallback` 被注册为 LLM 一级工具**，应仅作程序化兜底。— `tools/__init__.py:1-3,58-75`
5. **【中】分层职责**：节点经平铺 state 强耦合（见代码设计 4）；无插件化注册，新增工具需改 3+ 处。
6. **【中】容错/可用性**：单点故障（某 LLM/搜索引擎）无降级；无结构化日志/链路追踪（"之前查不到无日志"痛点未根治）。
7. **【中】并发/性能**：后端多用户并发安全性未验证；无连接池/限流/超时治理。
8. **【中】配置/部署**：`config.py` 是否多环境、密钥管理；`setup_pg*.ps1` 必要性；`main.py` 如何 uvicorn 启动未明确。
9. **【中】数据流一致性**：citation 编号、最终答案与引用一致性仅靠 `retrieve` 重编号保证，跨非 `__citations__:` 路径有风险。
10. **【低】DESIGN 与实现偏差**：batch_search/extract 等能力文档描述但实际无对应实现。

---

## 建议的修复优先级路线

- **第一轮（保正确 + 不崩，P 级）**：P1 引擎改异步/线程包裹；P2 兜底结合语义充分性而非仅布尔；P3/P4 API 与 openFDA 输入校验；P5 异常结构化不吞；P9 设 `recursion_limit`；P10 修 `FAILURE_MARKERS` 与以 `result.success` 为准；P7 加"无法回答"出口。
- **第二轮（成本 + 质量）**：P6 plan 上限/去重/失效源记忆；P8 程序化强制兜底；策略 6/14 Tavily 预算与缓存；P12 置信度透出前端。
- **第三轮（可维护 + 架构）**：P11 抽 `BaseSourceTool` 工厂；P3(架构) 引擎异步化；文档与代码对齐；补端到端测试与监控日志。

> 以上为纯审查输出，未修改任何业务代码。需要我按某一优先级开始修复时再告知。
