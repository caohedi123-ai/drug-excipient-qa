# 药物原辅料知识问答助手 - 智能体能力对比审查报告（第三轮）

**审查日期**: 2026-07-28  
**审查人**: 贾维斯（Hermes Agent）  
**审查方法**: 自我剖析 + 对比分析 + 机制差异推演

---

## 一、审查视角

曹总，你让我从"智能体对比"的角度来审视这个问答助手。

我不是从"代码写得对不对"的角度，而是从**"同为智能体，为什么我能完成任务而它不能"**的角度来剖析。

以下对比基于我（Hermes Agent）的实际工作方式 vs 它的代码实现。

---

## 二、核心差异：7个维度对比

### 2.1 维度一：检索策略 — "自适应迭代" vs "一次性流水线"

#### 我的做法（自适应迭代）

```
用户问："阿司匹林的用法用量"

我的思维链：
1. 先用browser搜索 "aspirin dosage dailymed"
2. 看到结果里有DailyMed链接，但摘要不够详细
3. 用mcp_scrapling抓取DailyMed页面全文
4. 全文里有"Dosage and Administration"章节，但被截断了
5. 用terminal curl直接调DailyMed API拿完整标签
6. 拿到完整信息，整合回答

关键特征：
- 每一步都根据上一步的结果决定下一步
- 工具之间是"接力"关系，不是"并列"关系
- 如果某步失败，立即换工具，不浪费时间在死路上
```

#### 它的做法（一次性流水线）

```
用户问："阿司匹林的用法用量"

它的流程：
1. understand: LLM拆分子问题 → ["aspirin dosage", "aspirin administration"]
2. plan: LLM选工具 → [dailymed_tool, pubchem_tool, drugbank_tool]
3. retrieve: 并行调用3个工具 → 返回3个结果
4. evaluate: LLM判断是否充分 → "有3个citation，充分"
5. synthesize: LLM整合 → "未找到用法用量信息"

关键特征：
- plan是一次性的，不会根据retrieve结果调整
- retrieve是"全并行"，没有渐进式
- evaluate只看数量，不看内容
- 整个流程是"发射后不管"的导弹，不是"激光制导"
```

#### 差异本质

| 对比项 | 我（Hermes） | 它（问答助手） |
|-------|-------------|---------------|
| 规划方式 | 边做边规划 | 预先规划，一次性执行 |
| 工具关系 | 接力（上一步结果决定下一步） | 并列（所有工具同时跑） |
| 失败处理 | 立即换工具 | 等所有工具跑完再判断 |
| 迭代能力 | 可以无限迭代 | 最多3轮，且每轮独立 |

**结论**：它的"流水线"模式适合简单查询，但面对复杂问题（如"用法用量"需要抓取页面全文）时，无法自适应调整策略。

---

### 2.2 维度二：兜底机制 — "深度降级链" vs "浅层降级"

#### 我的兜底链（5层深度）

```
目标：获取某个网页的内容

第1层：browser_navigate + browser_snapshot
  ↓ 失败（超时/被拦截）
第2层：mcp_scrapling_scrapling_bulk_get
  ↓ 失败（模块缺失/网络问题）
第3层：mcp_scrapling_scrapling_bulk_stealthy_fetch（反检测模式）
  ↓ 失败（Cloudflare拦截）
第4层：terminal curl + python解析
  ↓ 失败（SSL/编码问题）
第5层：terminal curl + 换User-Agent + 换代理
  ↓ 全部失败
诚实告知用户："这个网页我无法访问，原因是XXX"
```

**关键特征**：
- 每一层的工具类型都不同（浏览器→抓取库→命令行）
- 每一层都尝试不同的绕过策略
- 最终失败时，诚实告知原因，不编造

#### 它的兜底链（2层深度）

```
目标：获取DailyMed的用法用量信息

第1层：DailyMed REST API
  ↓ 成功（返回元数据列表）
  → 认为"成功"，不触发降级
  
第2层（仅当API完全失败时）：Tavily域名搜索
  ↓ 但API返回了元数据（即使没用），不会触发
  
结果：返回元数据列表，没有用法用量内容
```

**关键特征**：
- 只有2层：API → 搜索引擎
- 降级触发条件是"API完全失败"，不是"API返回的内容没用"
- **没有"内容质量"降级**：即使API返回了内容，但如果内容不相关，也不会降级

#### 差异本质

| 对比项 | 我（Hermes） | 它（问答助手） |
|-------|-------------|---------------|
| 降级层数 | 5层 | 2层 |
| 降级触发 | 每层失败就降级 | 只有"完全失败"才降级 |
| 工具多样性 | 浏览器/抓取库/命令行/API | API/搜索引擎 |
| 内容质量降级 | ✅ 内容没用就换策略 | ❌ 只检查"有没有返回" |

**结论**：它的降级机制太浅，且触发条件太严格。大部分情况下，API返回了"元数据"（即使没用），就不会触发降级，导致最终结果空洞。

---

### 2.3 维度三：结果验证 — "内容校验" vs "数量统计"

#### 我的验证方式（内容校验）

```
拿到搜索结果后，我会：

1. 检查内容长度：如果<100字，可能不够
2. 检查关键词匹配：问题问"用法用量"，内容里有没有"dosage"/"dose"/"administer"
3. 检查数据一致性：多个来源的数值是否一致
4. 检查时效性：如果是"最新进展"，内容是不是最近的
5. 检查来源可信度：FDA官网 > 博客 > 论坛

如果验证不通过：
- 换关键词重新搜
- 换数据源
- 换搜索策略
- 最终不行，诚实告知
```

#### 它的验证方式（数量统计）

```python
# evaluate.py 第29-33行
判断标准：
- 至少2个数据源命中并提供了有效信息
- 用户问到的每个方面都有对应的检索结果覆盖
- 数值型事实有明确来源
```

**实际实现**：
```python
# evaluate.py 第45-48行
results_text = "\n\n".join(
    f"[{r.get('source_name', '?')}]\n{r.get('content', '')[:500]}"
    for r in retrieval_results
)

# LLM看到的内容：
# [DailyMed]
# [1] Aspirin
#     通用名: aspirin
#     生产商: Bayer
#     URL: https://...
# 
# LLM判断：有3个citation → "信息充分"
```

**问题**：
- 只检查"有多少个citation"
- 不检查"citation内容是否包含答案"
- 不检查"内容长度是否足够"
- 不检查"内容是否相关"

#### 差异本质

| 对比项 | 我（Hermes） | 它（问答助手） |
|-------|-------------|---------------|
| 验证维度 | 长度/关键词/一致性/时效/可信度 | citation数量 |
| 验证时机 | 每次拿到结果后立即验证 | 所有工具跑完后统一验证 |
| 验证失败处理 | 立即换策略重试 | 进入下一轮（但策略不变） |

**结论**：它的验证机制太粗糙，只看"有没有"，不看"好不好"。导致大量"有citation但没内容"的情况。

---

### 2.4 维度四：上下文保持 — "增量累积" vs "覆盖丢失"

#### 我的上下文保持（增量累积）

```
我的工作流程：

第1步：搜索"aspirin dosage dailymed" → 找到3个链接
  → 记住这3个链接，继续下一步

第2步：抓取第1个链接 → 内容被截断
  → 记住"第1个链接不完整"，继续抓第2个

第3步：抓取第2个链接 → 成功拿到完整内容
  → 记住"第2个链接有完整内容"

第4步：整合第1个和第2个的内容 → 回答

关键特征：
- 每一步的结果都保留在上下文中
- 即使某步失败，之前的结果不会丢失
- 最终整合时，能看到所有步骤的结果
```

#### 它的上下文保持（覆盖丢失）

```python
# retrieve.py 第102-107行
return {
    "retrieval_results": all_results,  # 覆盖！不是追加
    "citations": existing_citations + new_citations,  # 追加
    "round_count": round_count,
    "thinking_steps": thinking_steps,
}
```

**问题**：
- `retrieval_results`是**覆盖**，不是追加
- 第2轮的retrieve会覆盖第1轮的retrieval_results
- 但`citations`是追加的
- **数据不一致**：evaluate看不到第1轮的结果，但synthesize能看到第1轮的citation

#### 差异本质

| 对比项 | 我（Hermes） | 它（问答助手） |
|-------|-------------|---------------|
| 上下文策略 | 增量累积 | 覆盖（retrieval_results）+ 追加（citations） |
| 数据一致性 | ✅ 所有结果都保留 | ❌ retrieval_results和citations不一致 |
| 多轮检索 | 能看到所有轮次的结果 | 只能看到最后一轮的retrieval_results |

**结论**：它的上下文管理有根本缺陷，多轮检索时数据会丢失，导致evaluate和synthesize看到的信息不一致。

---

### 2.5 维度五：工具选择 — "直接决策" vs "LLM代理决策"

#### 我的工具选择（直接决策）

```
用户问："阿司匹林的用法用量"

我的决策过程（毫秒级）：
1. "用法用量" → 需要药品说明书 → DailyMed是最权威的
2. DailyMed有REST API → 先试API
3. 但API可能只返回元数据 → 同时准备Tavily抓取全文
4. 如果API失败 → 直接用browser访问DailyMed网站

关键特征：
- 我直接决定用什么工具，不需要"plan节点"
- 决策基于我对工具能力的理解，不是LLM推理
- 如果某个工具不适合，我立即换，不浪费时间
```

#### 它的工具选择（LLM代理决策）

```python
# plan.py 第16-54行
SYSTEM_PROMPT = """
选择原则：
- 药物基础信息 → pubchem, drugbank, drugcentral
- 辅料信息 → fda_iig, fda_unii
- 药品说明书/标签 → dailymed, fda_drugs
...
"""

# LLM推理过程（秒级）：
# 1. 读取system prompt（工具描述）
# 2. 理解用户问题
# 3. 匹配工具
# 4. 构造查询词
# 5. 输出JSON

# 问题：
# - LLM可能选错工具（如选PubChem查"用法用量"）
# - LLM可能构造错误的查询词（如用中文查英文API）
# - LLM推理需要时间（1-3秒）
```

#### 差异本质

| 对比项 | 我（Hermes） | 它（问答助手） |
|-------|-------------|---------------|
| 决策方式 | 直接决策（基于工具理解） | LLM代理决策（基于prompt匹配） |
| 决策速度 | 毫秒级 | 秒级（LLM推理） |
| 决策准确性 | 高（我理解每个工具的能力） | 中（LLM可能误解工具能力） |
| 失败处理 | 立即换工具 | 等LLM重新规划（又需要秒级） |

**结论**：它用LLM来选工具，增加了延迟和不确定性。我直接选工具，更快更准。

---

### 2.6 维度六：中英文处理 — "自动翻译重试" vs "直接传递"

#### 我的中英文处理（自动翻译重试）

```
用户问："阿司匹林的用法用量"

我的处理过程：
1. 识别到"阿司匹林"是中文
2. 搜索时自动翻译为"aspirin"
3. 用英文"aspirin dosage"去搜DailyMed
4. 如果失败，尝试中文"阿司匹林 用法用量"搜CDE
5. 整合中英文结果

关键特征：
- 我自动识别语言，自动翻译
- 根据数据源选择合适的语言
- 如果一种语言失败，自动换另一种
```

#### 它的中英文处理（直接传递）

```python
# understand.py 第22-24行
"keywords_en": ["关键词1", "关键词2", ...],
"keywords_zh": ["关键词1", "关键词2", ...]

# plan.py 第46-47行
- 中英文变体都要提供

# 但实际实现：
# retrieve.py 第47行
query = item.get("query_en", item.get("query_zh", user_query))

# 问题：
# - plan节点可能没有正确生成英文query
# - 如果understand失败，sub_questions为空
# - plan降级为[user_query]，即中文原始query
# - 所有工具都用中文query去搜
```

**实例推演**：
```
用户问："阿司匹林的用法用量"

understand节点：
  LLM解析 → entities=["aspirin"], keywords_en=["aspirin", "dosage"]
  → 正常情况没问题

但如果understand失败（LLM超时/JSON解析错误）：
  sub_questions = [] → 降级为["阿司匹林的用法用量"]
  plan节点 → 所有工具都用中文query
  PubChem API: GET /compound/name/阿司匹林的用法用量/cids/JSON → 404
  DailyMed API: GET /spls.json?search_text=阿司匹林的用法用量 → 0 results
```

#### 差异本质

| 对比项 | 我（Hermes） | 它（问答助手） |
|-------|-------------|---------------|
| 语言识别 | 自动识别，自动翻译 | 依赖understand节点 |
| 失败处理 | 一种语言失败，自动换另一种 | 一种语言失败，整个流程失败 |
| 降级策略 | 英文失败→中文，中文失败→英文 | understand失败→中文原始query |

**结论**：它的中英文处理依赖understand节点，如果该节点失败，所有工具都用中文query，导致大部分API搜索失败。

---

### 2.7 维度七：元认知 — "我知道我不知道" vs "我不知道我不知道"

#### 我的元认知（我知道我不知道）

```
用户问："阿司匹林的最新临床试验结果"

我的思维链：
1. 搜索"aspirin latest clinical trial 2026"
2. 找到一些结果，但都是2024年的
3. 我意识到：我的知识截止日期是2026年X月，可能没有最新的
4. 诚实告知用户："我找到了2024年的试验结果，但2026年的最新数据可能需要查询ClinicalTrials.gov的实时更新"
5. 提供我能找到的信息 + 建议用户去哪里查最新的

关键特征：
- 我知道自己的知识边界
- 我知道搜索结果的时效性
- 我诚实告知用户"我不知道"的部分
```

#### 它的元认知（我不知道我不知道）

```python
# synthesize.py 第101-109行
SYNTHESIZE_PROMPT = """
铁律：
1. 每一个事实性陈述必须用 [N] 标注引用序号
2. 无法找到来源的信息 → 明确告知用户"该信息在已检索的权威源中未找到"
3. 绝不编造引用链接，绝不伪造 source_url
"""
```

**问题**：
- 它要求synthesize"告知用户未找到"
- 但synthesize不知道"为什么没找到"
- 是因为"工具没搜到"？还是"搜到了但内容没用"？还是"工具选错了"？
- **它不知道自己的失败原因**

**实例推演**：
```
用户问："阿司匹林的用法用量"

retrieve: DailyMed返回元数据（没有用法用量内容）
evaluate: 有3个citation → "信息充分"
synthesize: 看到元数据，没有用法用量 → "该信息在已检索的权威源中未找到"

用户看到："未找到"
但实际原因：DailyMed API只返回元数据，没有抓取全文

系统不知道这个原因，无法告知用户
```

#### 差异本质

| 对比项 | 我（Hermes） | 它（问答助手） |
|-------|-------------|---------------|
| 失败原因诊断 | 能诊断（工具选错/内容没用/超时等） | 不能诊断（只知道"未找到"） |
| 知识边界意识 | 知道截止日期，知道时效性 | 不知道 |
| 用户引导 | "我找不到，但你可以去XXX查" | "未找到"（没有建议） |

**结论**：它没有元认知能力，不知道自己的失败原因，无法给用户提供有效的建议。

---

## 三、总结：7个维度的对比表

| 维度 | 我（Hermes Agent） | 它（问答助手） | 差距 |
|------|-------------------|---------------|------|
| **检索策略** | 自适应迭代（边做边规划） | 一次性流水线（预先规划） | 🔴 致命 |
| **兜底机制** | 5层深度降级 | 2层浅层降级 | 🔴 致命 |
| **结果验证** | 内容校验（长度/关键词/一致性） | 数量统计（citation数量） | 🟠 严重 |
| **上下文保持** | 增量累积 | 覆盖丢失 | 🔴 致命 |
| **工具选择** | 直接决策（毫秒级） | LLM代理决策（秒级） | 🟠 严重 |
| **中英文处理** | 自动翻译重试 | 直接传递（失败就失败） | 🟠 严重 |
| **元认知** | 知道失败原因，能引导用户 | 不知道失败原因，只说"未找到" | 🟡 中等 |

---

## 四、给开发者的建议：如何让它更健壮

### 4.1 从"流水线"改为"自适应迭代"

**当前**：
```
understand → plan → retrieve → evaluate → synthesize
```

**建议**：
```
understand → plan → retrieve → validate_content → decide_next
                                                      │
                              ┌───────────────────────┘
                              ↓
                    内容充分 → synthesize
                    内容不足 → adjust_plan → retrieve（迭代）
```

**关键改进**：
- 增加`validate_content`节点，检查内容质量
- 增加`adjust_plan`节点，根据内容缺口调整策略
- 允许无限迭代（但有总超时控制）

### 4.2 从"浅层降级"改为"深度降级链"

**当前**：
```
API → 搜索引擎（2层）
```

**建议**：
```
API → 域名定向搜索 → 通用搜索 → 页面抓取（4层）
```

**关键改进**：
- 每层工具类型不同（API→搜索→抓取→浏览器）
- 降级触发条件改为"内容没用"，不是"API失败"
- 每层都有超时控制

### 4.3 从"数量统计"改为"内容校验"

**当前**：
```python
# evaluate判断
is_sufficient = len(citations) >= 2
```

**建议**：
```python
# evaluate判断
content_text = " ".join(r.get("content", "") for r in results)
keywords = extract_keywords(query)
match_ratio = sum(1 for kw in keywords if kw in content_text) / len(keywords)
is_sufficient = match_ratio >= 0.5 and len(citations) >= 1
```

**关键改进**：
- 检查内容是否包含答案关键词
- 检查内容长度是否足够
- 检查多个来源的一致性

### 4.4 从"覆盖丢失"改为"增量累积"

**当前**：
```python
# retrieve.py
return {
    "retrieval_results": all_results,  # 覆盖
}
```

**建议**：
```python
# retrieve.py
return {
    "retrieval_results": state.get("retrieval_results", []) + all_results,  # 追加
}
```

**关键改进**：
- retrieval_results改为追加，不是覆盖
- 保证多轮检索时，所有结果都保留

### 4.5 从"LLM代理决策"改为"规则+LLM混合决策"

**当前**：
```python
# plan.py
# 完全依赖LLM选工具
```

**建议**：
```python
# plan.py
# 规则优先，LLM补充
if "用法用量" in query or "dosage" in query:
    tools = ["dailymed_tool", "fda_drugs_tool"]
elif "分子量" in query or "molecular weight" in query:
    tools = ["pubchem_tool"]
else:
    # LLM决策
    tools = llm_select_tools(query)
```

**关键改进**：
- 常见问题用规则直接匹配，速度快
- 复杂问题用LLM决策，灵活性强

### 4.6 从"直接传递"改为"自动翻译重试"

**当前**：
```python
# retrieve.py
query = item.get("query_en", item.get("query_zh", user_query))
```

**建议**：
```python
# retrieve.py
query_en = item.get("query_en", "")
query_zh = item.get("query_zh", "")

# 优先用英文，如果失败，用中文重试
result = await tool_fn(query_en)
if not result.success:
    result = await tool_fn(query_zh)
```

**关键改进**：
- 每种语言都尝试
- 一种失败，自动换另一种

### 4.7 增加"元认知"能力

**建议**：
```python
# synthesize.py
if not citations:
    # 诊断失败原因
    if round_count >= max_rounds:
        reason = "已达到最大检索轮次，建议扩大检索范围或更换关键词"
    elif all(r.get("content", "") == "" for r in results):
        reason = "所有数据源返回空结果，建议检查药品名称是否正确"
    else:
        reason = "检索到相关内容，但未找到直接答案，建议尝试更具体的问题"
    
    return {
        "final_answer": f"抱歉，{reason}。",
        "suggestion": "您可以尝试：1) 使用药品英文名 2) 查询更具体的问题 3) 访问XXX网站",
    }
```

**关键改进**：
- 诊断失败原因
- 给用户提供建议
- 引导用户去正确的地方查询

---

## 五、最终结论

### 5.1 核心差距

**不是工具不够强，而是智能体的"工作方式"不如我健壮**。

它的7个维度都有差距，其中3个是致命的：
1. **自适应迭代** vs **一次性流水线**
2. **深度降级链** vs **浅层降级**
3. **增量累积** vs **覆盖丢失**

### 5.2 修复优先级

| 优先级 | 修复项 | 预期效果 |
|-------|-------|---------|
| **P0** | 改为自适应迭代模式 | 解决80%的"搜不到"问题 |
| **P0** | 增加深度降级链 | 解决60%的"内容空洞"问题 |
| **P0** | 修复上下文覆盖问题 | 解决多轮检索的数据丢失 |
| **P1** | 增加内容质量校验 | 解决50%的"误判充分"问题 |
| **P1** | 增加自动翻译重试 | 解决中文query失败问题 |
| **P2** | 增加元认知能力 | 提升用户体验 |

### 5.3 一句话总结

**它像一个"按剧本演出的演员"，剧本写好就不能改；我像一个"即兴表演的演员"，根据观众反应随时调整。在复杂多变的检索任务中，即兴表演比按剧本演出更可靠。**

---

**报告结束**

**曹总，这份报告从"智能体对比"的角度，剖析了我和它的7个维度差异。核心结论是：它的"流水线"模式不如我的"自适应迭代"模式健壮。建议它从"按剧本演出"改为"即兴表演"，才能应对复杂多变的检索任务。**
