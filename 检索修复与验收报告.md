# 检索管线修复与验收报告

> 目标：按 DESIGN.md 的 24+ 权威源方案补齐工具、修复"配了工具却查不到"的真实根因、建立三层兜底、自测通过。
> 状态：代码层全部修复 + 自测全通过。真实验收需在你有外网 + 已配置 API Key 的环境进行（当前沙箱离线）。

## 一、为什么之前"配了工具还查不到"——真实根因

排查中挖出**多个叠加的真实 bug**，并非工具没配：

1. **【最关键】AnySearch 引擎每次调用都静默崩溃**
   `anysearch_engine.py` 的 `anysearch_vertical` 返回 `SearchResult` 时：
   - 缺必填参数 `source_name`
   - 多传了 `SearchResult` 根本不存在的 `metadata` 参数
   - 三处 `Citation` 创建漏必填 `id`
   
   结果：每次调 AnySearch 都抛 `TypeError`，被各源内部 `try/except` 吞掉 → 返回"未找到"。这就是"配了 AnySearch 泛搜却一点都查不到"的直接原因。已修复。

2. **`success` 被硬编码为 `True`**（`retrieve.py`）
   工具返回"[X] 未找到…/调用失败…"这类失败字符串也被记为 `success=True`，导致质量评估失真、该兜底时不兜底。已改为按内容真实判定。

3. **兜底工具从未接入主流程**
   `anysearch_fallback` 兜底工具根本没注册；`tools/__init__.py` 仅注册 12 个工具。LLM 选不到泛搜兜底。已补齐 30 个工具并注册兜底。

4. **工具描述两处不一致**
   `plan.py` 与 `adjust.py` 使用的工具描述各自硬编码 12 个、互不一致，重规划时 LLM 看不到兜底工具。已统一为 `tools` 模块动态生成（30 个）。

## 二、24+ 工具补齐（共 30 个，含兜底）

| 分组 | 工具 |
|---|---|
| 药物基础信息 | pubchem, drugbank, drugcentral, chembl(新), coconut(新) |
| 辅料与制剂 | fda_iig, fda_unii, fda_ndc(新), dailymed, fda_orange(新) |
| 注册审批 | fda_drugs, ema(新), cde, pmda(新) |
| 安全与相互作用 | fda_faers, sider(新), ddinter(新) |
| 靶点与基因 | open_targets(新), bindingdb(新), pharmgkb(新), ttd(新) |
| 分类标准与指南 | rxnorm, who_atc(新), ich(新) |
| 文献/专利/综合 | pubmed, espacenet(新), cnipa(新), wikipedia, clinicaltrials(新) |
| 兜底 | anysearch_fallback_tool(新) |

每个工具：**API 优先 + AnySearch/Tavily 降级**，输出统一 `[Name] content\n\n__citations__: [...]`，失败优雅返回"未找到"。

## 三、三层兜底策略（每层都 OK）

1. **工具级**：每个源 API 优先，失败自动降级 AnySearch/Tavily（17 个新工具均实现）。
2. **轮次级（硬保底）**：`retrieve.py` 内，本轮所有选定工具零有效结果时，**自动调用 AnySearch 泛搜兜底**——保证"配了泛搜就一定用得上"，不依赖 LLM 是否选到。
3. **决策级**：`decide.py` 判不足时建议引入兜底工具；`plan.py` 选择原则明确"专有源不足务必用 `anysearch_fallback_tool`"；`adjust` 重规划时 LLM 可选用。

## 四、自测结果（mock，不依赖网络）——全部通过

运行 `python backend/test_retrieval_pipeline.py`：

- [OK] 注册工具数=30（含 anysearch_fallback_tool 兜底）
- [OK] 全部 30 个工具均可被 retrieve 调用
- [OK] prompts 工具描述包含 anysearch_fallback_tool（30 工具统一描述）
- [OK] 失败工具 success=False 判定正确（修复硬编码 bug）
- [OK] 轮次级 AnySearch 兜底触发且 success=True
- [OK] 兜底产生引用数=1
- [OK] 专有源成功场景 success=True 且保底未误触发
- [OK] AnySearch 引擎失败路径健壮（Citation 含 id，不再 TypeError）
- === 检索管线自测全部通过 ===

附加验证：`build_graph()` 成功（7 节点，`ToolNode` 校验全部 30 工具格式）；`import main` 无导入错误。

## 五、真实验收步骤（你这边有网环境）

当前沙箱**离线**（AnySearch/Tavily/各 API 均 DNS 不可达），无法在此跑"每个工具真实联网返回"的端到端。请在有外网且已配置 API Key（DEEPSEEK / ANYSEARCH / TAVILY 等）的环境：

1. 启动后端：`cd backend && python main.py`
2. 前端提问，预期行为：
   - 即使专有源失败，thinking 步骤出现 `③ 兜底: 所选数据源均无结果，已自动启用 AnySearch 泛搜保底`，最终有来源答案而非"未找到"。
   - 后端日志 `backend.out.log` 可见每轮每个工具的 success / 失败原因。
3. 随时复跑自测确认逻辑层：`python backend/test_retrieval_pipeline.py`

## 六、改动文件清单

新增（17 工具 + 1 兜底 + 1 自测）：
`tools/sources/{chembl,coconut,fda_ndc,fda_orange,ema,pmda,sider,ddinter,open_targets,bindingdb,pharmgkb,ttd,who_atc,ich,espacenet,cnipa,clinicaltrials,anysearch_fallback}.py`、`test_retrieval_pipeline.py`

修改（核心修复）：
- `tools/engines/anysearch_engine.py`：补 `source_name`、去 `metadata`、三处 `Citation` 补 `id`
- `agent/nodes/retrieve.py`：真实 `success` 判定 + 轮次级 AnySearch 硬保底 + 调用兼容 async/sync
- `tools/__init__.py`：注册全部 30 工具、动态生成描述、延迟 import ToolNode
- `agent/prompts.py`：委托 `tools` 模块统一描述
- `agent/nodes/plan.py`：选择原则扩展并强调兜底
- `agent/nodes/decide.py`：不足时追加兜底建议
