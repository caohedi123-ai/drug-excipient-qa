# 多会话并发修复与验证报告

## 一、目标

多设备登录、同时开多个对话，系统不崩、消息正常执行、内容互不干扰。

## 二、根因（并发互踢）

| 位置 | 问题 |
| --- | --- |
| `frontend/src/lib/api.ts` | 模块级全局 `activeController`，`sendChatMessage` 入口无条件 abort 上一个请求 → 跨会话互相中止 |
| `frontend/src/components/ChatContainer.tsx` | 单组件实例直接 `setMessages/setStreamingContent` 等全局状态，后台会话的回调污染当前会话 UI |

后端无锅：`backend/agent/graph.py` 锁仅初始化用，`/api/chat` 无全局锁，已确认并发安全。

## 三、修复方案

### 1. 按会话请求隔离（`api.ts`）

- 全局 `activeController` → `controllers = new Map<string, AbortController>()`，`requestKey = thread_id`。
- 同会话新请求顶替旧请求（会话内串行），跨会话并行互不 abort。
- 新增 `isStale()`：请求被同 key 新请求顶替后静默退出，不触发任何终态回调。
- `abortActiveRequest(key)` 只中止指定会话；无参调用中止全部（**不 clear map**，避免 isStale 误判导致收尾悬挂——E2E 发现并修复）。

### 2. 统一状态写入入口（`ChatContainer.tsx`）

- 新增 `applyConvState(convId, updater)`：当前会话直接 setState；后台会话写入 `savedStatesRef` 缓存，切回时恢复，保证流式内容各归其位。
- 所有异步回调（onStep/onSources/onToken/onDone/onError）统一走该入口。
- 会话切换 useEffect 增加一致性校验，历史加载不覆盖本地新消息。
- `App.tsx`：新建会话自动激活（`setActiveId(prev => prev ?? conv.id)`）。

## 四、验证矩阵

| 层级 | 方式 | 结果 |
| --- | --- | --- |
| 单元/隔离 E2E（无浏览器） | Node 24 原生 strip-types 跑 `api.ts`，mock `localStorage` + fetch/SSE 流，5 组场景：跨 key 并行互不 abort、同 key 互踢旧请求静默退出、按 key 中止、全局中止收尾、HTTP 错误隔离 | 5/5 PASS（EXIT=0） |
| 前端构建 | `npm run build`（tsc + vite） | EXIT=0 |
| 后端并发冒烟 | 两会话（阿司匹林 vs 对乙酰氨基酚）并行 `POST /api/chat`，SSE 全解析 | A ok(len=1636,steps=7)、B ok(len=1731,steps=7)，总墙钟 164.4s |
| 内容互不干扰 | 交叉污染检查：A 含"对乙酰氨基酚"=False，B 含"阿司匹林"=False | 通过 |
| 服务重启回归 | 重启前端 vite（5173）与后端（18082，`backend/.venv` 为 Unix 布局不可用，用全局 Python311 + backend 目录启动） | 健康检查 200；登录/会话列表/me 全 200；历史会话数据不丢 |
| 重启后并发复测 | 复用并发冒烟脚本 | 两会话均完成、无交叉污染（exit 0） |

## 五、结论

- 跨会话互踢根因已消除：请求级 controller 按 thread_id 隔离，同会话串行、跨会话并行。
- 会话状态写入收敛到统一入口，后台会话不再污染当前会话 UI。
- 重启后两会话真实并行完成、内容互不干扰、系统未崩，符合验收标准。

## 六、附注

- 本任务全程未使用 playwright（按用户要求改用轻量 node + mock fetch 方案）。
- 后端 `/api/health` 显示 checkpointer 为 memory 降级（缺 psycopg 二进制包），属既有环境问题，不影响多会话并发功能；如需持久化 agent 会话图状态可另行安装 `psycopg[binary]`。
- 服务当前状态：前端 `http://127.0.0.1:5173`、后端 `http://127.0.0.1:18082` 运行中。
