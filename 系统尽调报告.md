# 药物原辅料知识问答助手 · 系统尽调报告

> 尽调范围：后端智能体检索编排全链路（LangGraph 节点、工具注册、状态管理、服务接口）
> 依据：`backend/agent/**`、`backend/tools/**`、`backend/config.py`、`backend/main.py` 源码逐文件勘察
> 结论：系统已具备「理解 → 计划 → 并行检索 → 评估 → 决策 → 调整 → 合成」的完整多轮闭环，鲁棒性设计分层到位；多轮对话记忆为"轻量注入式"，存在可预期短板（详见第三章）。

---

## 一、工具清单及每个工具的描述、方法

### 1.1 工具总览（31 个）

系统通过 `backend/tools/__init__.py` 中的 `TOOLS_BY_NAME` 注册表统一管理工具。全部工具均为 **LangChain `@tool` 异步函数**，统一签名为：

```python
async def xxx_tool(query: str) -> str
```

调用协议：输入为单个字符串 `query`（可由用户问题、子问题、扩充查询词构成）；输出为标准字符串，成功时形如：

```
[源名] 检索到的正文内容……
__citations__: [{"source_name": ..., "title": ..., "url": ..., "published_date": ...}, ...]
```

其中 `__citations__:` 为工具向检索层回传引用元数据的**结构化协议**，`retrieve` 节点会解析并重新编号。失败/无结果时返回 `"[源名] 未找到 'query' 的相关信息。"`。

每个工具的描述（docstring）会经 `get_tool_descriptions()` 动态拼入 `plan`/`adjust` 节点的系统提示词，并带有 **[官方API] / [网络检索] / [综合检索]** 三档数据成色标签（`SOURCE_TYPE` 映射），帮助 LLM 区分一手权威数据与联网兜底来源。

### 1.2 分组成清单

按业务域分为 **8 组 31 个工具**：

| 组别 | 工具名 | 数据成色 | 描述 | 调用方法 |
|---|---|---|---|---|
| **药物基础信息** | `pubchem_tool` | [官方API] | 化合物基础信息：分子量、分子式、SMILES、LogP、CAS、IUPAC、结构 | 直连 PubChem REST PUG；CID 名称解析失败时降级取首词、再降级 Tavily 域名搜索；支持中英文 |
| | `drugbank_tool` | [网络检索] | 药物概览（靶点/适应症/相互作用摘要） | Tavily 域名定向搜索 go.drugbank.com（非官方 API，成色低） |
| | `drugcentral_tool` | [官方API] | 监管状态、临床试验、文献整合数据 | 直连 DrugCentral API |
| | `chembl_tool` | [官方API] | ChEMBL ID 与基础化学信息；MCP 接入后可取靶点/机制/ADMET | 直连 ChEMBL REST（`backend/tools/sources/chembl_mcp_client.py` 提供 MCP 增强通道） |
| | `coconut_tool` | [官方API] | 天然产物（COCONUT）数据 | 直连 COCONUT API |
| **辅料与制剂** | `fda_iig_tool` | [官方API] | FDA 批准辅料：名称、给药途径、最大用量、剂型 | 直连 openFDA 辅料数据库 |
| | `fda_unii_tool` | [官方API] | FDA UNII 唯一标识符 | 直连 openFDA UNII |
| | `fda_ndc_tool` | [官方API] | FDA NDC 国家药品代码：包装、剂型、标签商 | 直连 openFDA NDC |
| | `dailymed_tool` | [官方API] | FDA 药品说明书：适应症、用法用量、不良反应、禁忌、辅料列表 | 直连 DailyMed SPL API |
| | `fda_orange_tool` | [官方API] | 橙皮书：治疗等效性（ABCD）、专利与独占期、RLD | 直连 openFDA Orange Book |
| **注册审批** | `fda_drugs_tool` | [官方API] | FDA 药品批准：申请号、批准日期、剂型、活性成分、标签 | 直连 openFDA Drugs |
| | `ema_tool` | [网络检索] | 欧盟 EMA 上市许可、EPAR、审评报告 | Tavily 域名定向搜索（非官方 API） |
| | `cde_tool` | [网络检索] | 中国 CDE 审评进度、受理号、注册分类 | 网络检索（中文源） |
| | `pmda_tool` | [网络检索] | 日本 PMDA 注册审评、副作用情报 | 网络检索 |
| **安全与相互作用** | `fda_faers_tool` | [官方API] | FAERS 不良事件报告、严重程度、发生率、ROR | 直连 openFDA FAERS |
| | `sider_tool` | [网络检索] | 不良反应、副作用频率、目标器官系统 | 网络检索（SIDER 风格） |
| | `ddinter_tool` | [网络检索] | 药物-药物相互作用（DDI）：联用风险、机制、严重程度 | 网络检索；输入示例 `"warfarin aspirin interaction"` |
| **靶点与基因组** | `open_targets_tool` | [官方API] | 靶点-疾病-药物关联证据、可药性 | 直连 Open Targets GraphQL API |
| | `bindingdb_tool` | [网络检索] | 蛋白质-配体结合亲和力：Ki/Kd/IC50 | 网络检索 |
| | `pharmgkb_tool` | [网络检索] | 药物基因组学：基因-药物关联、CPIC 指南 | 网络检索 |
| | `ttd_tool` | [网络检索] | 治疗靶点、靶点-药物关联、可药性蛋白 | 网络检索 |
| **分类标准与指南** | `rxnorm_tool` | [官方API] | RxNorm 标准术语：RxCUI、语义类型、术语关系、ATC 映射 | 直连 RxNorm API |
| | `who_atc_tool` | [网络检索] | WHO ATC 分类代码、层级、同类对照 | 网络检索 |
| | `ich_tool` | [网络检索] | ICH 指南、质量/安全/有效性规范 | 网络检索 |
| **文献、专利与综合** | `pubmed_tool` | [官方API] | 生物医学文献：论文、综述、临床试验、MeSH | 直连 NCBI E-utilities |
| | `espacenet_tool` | [网络检索] | 全球药物/辅料专利（EPO）：化合物/工艺/晶型专利 | 网络检索 |
| | `cnipa_tool` | [网络检索] | 中国专利（CNIPA）：发明专利、制剂工艺 | 网络检索（中文源） |
| | `wikipedia_tool` | [网络检索] | 药物综述背景信息 | 网络检索 |
| | `clinicaltrials_tool` | [官方API] | 临床试验注册：设计、阶段、入排标准、结果 | 直连 ClinicalTrials.gov API v2 |
| **兜底** | `anysearch_fallback_tool` | [网络检索] | 全网不限域泛搜（health/academic/ip 垂直域），保证最终有答案 | AnySearch 聚合引擎（`backend/tools/engines/anysearch_engine.py`） |
| **综合速查** | `excipient_basic_info_tool` | [综合检索] | 原辅料一站式速查：并行调用 16 个数据源底层函数，产品类型路由、置信度分级、三层降级 | 迁移自 jiansuo3 检索内核；复用各源 `_search_xxx` 底层函数 + 本工具内关键词扩展/实体解析/CAS 验证；条件导入，失败不影响其他工具 |

> 备注：另有 `backend/tools/engines/` 下的通用引擎（AnySearch / Tavily / Exa 等），它们是被各源工具复用的底层检索能力，不直接向 LLM 暴露。

---

## 二、整合阶段的流程

### 2.1 编排拓扑（9 节点 LangGraph 状态图）

```
        ┌────────────────────────────────────────────────────────────┐
        │                    AgentState（含 messages reducer）       │
        └────────────────────────────────────────────────────────────┘
                          │
                    ┌─────▼─────┐
 START ───────────► │ understand │  实体/意图/子问题/中英文关键词；闲聊短路
                    └─────┬─────┘
                          │ intent == "chat"  →  skip_retrieval=True → END（礼貌拒绝）
                          │
                    ┌─────▼─────┐
                    │    plan    │  LLM 按意图选工具 + 差异化查询词（上限 8）
                    └─────┬─────┘
                          │
                    ┌─────▼──────┐
                    │  retrieve   │  并行 gather 调用；解析 __citations__；
                    │             │  零有效结果 → 自动 AnySearch 兜底
                    └─────┬──────┘
                          │
                    ┌─────▼──────┐
                    │  evaluate   │  LLM 语义评估 + 规则质量分 _calc_content_quality
                    └─────┬──────┘
                          │
                    ┌─────▼──────┐
                    │   decide    │  白盒路由（见 2.3）
                    └─────┬──────┘
          ┌───────────────┼──────────────────┐
          │               │                  │
   is_sufficient      不足 且            达上限仍不足
   （充分）           round < max          （max=3 轮）
          │               │                  │
   ┌──────▼──────┐  ┌─────▼────────┐  ┌──────▼─────────┐
   │  synthesize  │  │ expand_queries│  │  final_search   │
   │  合成回答     │  │  查询词扩充    │  │  末轮 AnySearch 补全│
   └──────┬──────┘  └─────┬────────┘  └──────┬─────────┘
          │               │                  │
          ▼               ▼                  ▼
        write_answer   ┌────▼────┐       ┌────▼─────┐
        messages/END   │ adjust  │       │ synthesize│──► END
                       │ 重规划   │       └──────────┘
                       └────┬────┘
                            ▼
                          plan（第二轮）
```

### 2.2 节点职责

| 节点 | 文件 | 职责与关键逻辑 |
|---|---|---|
| `understand` | `nodes/understand.py` | 10 类意图分类（drug_info / excipient_info / registration / safety / mechanism / interaction / literature / comparison / general / **chat**）；实体识别（英文名优先）；子问题拆分（按"药名+动词+限定词"，中英各一）；注入最近 2 轮对话历史做**指代消解**；闲聊直接返回空引用并短路 |
| `plan` | `nodes/plan.py` | LLM 依据意图→工具映射表选工具（上限 8 个）+ 为每工具生成**差异化查询词**；`_validate_plan` 去重、剔除未注册工具、剔除已失败源、裁剪超限 |
| `retrieve` | `nodes/retrieve.py` | `asyncio.gather` 并行调用选中工具；解析 `__citations__` 并重编号；失败标记（超时/异常/空）累积到 `failed_tools`；本轮零有效结果或 `force_fallback` 时**自动 AnySearch 兜底**；累积 `retrieval_results/citations/failed_tools/search_history` |
| `evaluate` | `nodes/evaluate.py` | LLM 判断 `is_sufficient/confidence/missing/suggestion`；并行计算规则质量分 `content_quality`（关键词匹配 0.5 + 内容长度 0.3 + 源数量 0.2）；LLM 异常时**降级用规则分判定**（≥0.6 视为充分） |
| `decide` | `nodes/decide.py` | 白盒路由：LLM 判定为主、规则分为安全网；`force_fallback` 程序化兜底（质量分<0.4 或 零成功源 或 失败源≥3 且未用过兜底时强制触发）；`round_count >= max_rounds` 防死循环 |
| `expand_queries` | `nodes/expand.py` | 查询词扩充：实体名穷举（化学名/商品名/代号/中文名）+ 按维度分解 + 工具→查询词映射，解决同义词死循环 |
| `adjust` | `nodes/adjust.py` | 基于 `missing_info` 重新规划：注入扩充结果与前几轮已用工具清单，强调**优先未用过的工具、换查询角度不与前几轮重复** |
| `final_search` | `nodes/final_search.py` | 末轮补全：LLM 生成 1-3 条 AnySearch query 泛搜；`final_search_done` 标记防重入；重算 `cannot_answer / low_confidence` |
| `synthesize` | `nodes/evaluate.py::run_synthesize` | 强引用铁律（每条事实标 [N]、不编造 URL、数值单标引用）、结论前置、`## 总结` 收尾、证据不足时追加"建议核实官方来源"；失败时降级为结果拼接 |

### 2.3 条件路由

| 条件 | 路由 | 说明 |
|---|---|---|
| `intent == "chat"` | understand → **END** | 闲聊短路，返回礼貌拒绝 + 空引用 |
| `is_sufficient == True` | decide → **synthesize** | 信息充分，直接合成回答 |
| `is_sufficient == False` 且 `round_count < max_rounds(3)` | decide → **expand_queries → adjust → plan** | 进入下一轮检索循环 |
| `is_sufficient == False` 且达 3 轮上限 | decide → **final_search → synthesize** | 末轮泛搜兜底后强制合成（含 cannot_answer 声明） |

### 2.4 多轮检索闭环

单次完整问题处理的最坏路径为：**理解 → 计划 → 检索 → 评估 → 决策**，最多循环 3 轮（每轮经 `adjust` 换工具/换查询词），末轮经 `final_search` 兜底后合成。每轮的 `retrieval_results / citations / failed_tools / search_history / thinking_steps` 均累积在 `AgentState` 中，供后续轮次与最终合成复用，避免重复检索与自相矛盾。

---

## 三、多轮对话是否存在短板

### 3.1 现状：轻量注入式记忆

多轮记忆由三部分构成：

1. **消息累积**：`AgentState.messages` 使用 `Annotated[list, operator.add]` reducer，每轮 user query 与 assistant answer 均追加；
2. **持久化**：graph 优先挂 `PostgresSaver` checkpointer（会话级 thread 隔离），PG 不可用时降级 `MemorySaver`；
3. **上下文注入**：`understand` 注入最近 2 轮（4 条消息，助手回答截断 200 字符）、`evaluate` 注入最近 2 条、`synthesize` 注入最近 4 条，供指代消解与追问一致性使用。

### 3.2 已识别的短板（按影响排序）

| # | 短板 | 具体表现 | 影响 | 建议 |
|---|---|---|---|---|
| 1 | **无显式记忆管理/会话压缩** | 无历史摘要（summarization）、无关键事实抽取、无记忆剪枝；全部依赖把 messages 原样注入 | 长会话（>10 轮）后 token 成本线性增长；超过注入窗口的早期上下文丢失 | 增加历史摘要节点或滚动压缩；对 messages 做 token 上限截断 |
| 2 | **注入窗口固定且偏窄** | understand 只看最近 2 轮、synthesize 只看最近 4 条 | 跨 2 轮以上的指代（"上次说的那个辅料"）可能失焦；多实体轮转讨论时选错主体 | 注入窗口可配置；对高频实体做会话级实体记忆 |
| 3 | **助手回答截断可能截掉关键信息** | `understand` 注入历史时助手回答截断到 200 字符 | 长回答的实体名/关键数值可能被截断，影响指代消解准确性 | 截断策略改为"保留实体与数值前缀"或抽取式摘要 |
| 4 | **依赖 PostgreSQL 可用性** | 优先 `PostgresSaver`，PG 故障时降级 `MemorySaver`（进程内存） | 重启进程即丢失全部会话历史；多实例部署时记忆不共享 | 降级时告警；或引入 Redis 持久化会话 |
| 5 | **闲聊短路后的历史污染** | chat 意图直接写回 messages（含礼貌拒绝） | 后续专业追问的历史注入会包含闲聊噪音 | 闲聊回复可单独标记，注入时过滤 |
| 6 | **无引用链一致性校验** | synthesize 引用基于当前轮 citations，未校验多轮间引用是否冲突 | 极端情况下不同轮来源矛盾时可能选择偏差 | 合成前对多轮 citations 做来源去重与冲突提示 |
| 7 | **无用户画像/偏好记忆** | 无跨会话记忆 | 同一用户反复询问相同主题时无法复用 | 属增强项，非当前需求 |

### 3.3 结论

多轮对话**基础可用**：追问指代（"它的分子量是多少"）经 understand 注入历史可正常消解；同一会话内多轮检索结果能通过 `search_history` 防重。但**没有做到"记忆即第一等公民"**——记忆只是"最近几条消息的注入"，长会话、跨轮远指代、进程重启场景存在明确短板，属中等风险，建议按上表 1-4 项优先加固。

---

## 四、系统鲁棒性

### 4.1 分层兜底架构

```
第 1 层  工具级    ：各源 API 优先，失败自动降级 AnySearch/Tavily（如 PubChem API→Tavily 域名）
第 2 层  轮次级    ：retrieve 节点内本轮零有效结果 → 自动 AnySearch 泛搜保底
第 3 层  决策级    ：decide 判不足 → plan 重选（含 anysearch_fallback_tool）
第 4 层  末轮级    ：final_search 用 LLM 生成的 1-3 条 query 做最后泛搜，保证"有答案而非未找到"
```

### 4.2 LLM 失败降级链（各节点 JSON 解析均受保护）

| 节点 | 异常降级策略 |
|---|---|
| `understand` | 解析失败 → 用默认实体/意图（general）继续 |
| `plan` | 解析失败 → 默认工具集 + 原查询词；`_validate_plan` 保证合法 |
| `evaluate` | LLM 异常 → 规则质量分判定（≥0.6 充分）；confidence 置 low |
| `decide` | LLM 判定异常 → 规则分 + 轮次白盒逻辑兜底 |
| `synthesize` | 合成异常 → 降级为按源拼接检索结果 |

### 4.3 其他鲁棒性设计

- **失败工具剔除**：`failed_tools` 跨轮累积，`adjust`/`plan` 明确回避，避免反复打空源；
- **死循环防护**：`round_count >= max_rounds` 强制转 `final_search`；`final_search_done` 防重入；
- **程序化 force_fallback**：质量分过低/零成功/失败源过多时绕过 LLM 直接泛搜，不让"LLM 误判充分"堵死出路；
- **速查工具错误隔离**：`excipient_basic_info_tool` 对 16 个源的崩溃/超时(60s)/异常/导入失败全部内部捕获；条件导入，失败不影响其余 30 个工具；
- **服务层韧性**：Redis 不可用不崩（chat 不依赖）；`sanitize_query` 查询消毒；JWT 认证（24h 过期、token 校验）；SSE 流式按句切块输出；PG/DB 连接独立管理；
- **请求约束**：query 长度 1-2000 字符（pydantic 校验）。

### 4.4 残留风险（如实披露）

1. **外部源依赖**：FDA/PubMed/ClinicalTrials 等公开 API 的限流与可用性不受控，高并发下可能抖动（有兜底但延迟增加）；
2. **网络检索源成色**：DrugBank/EMA/CDE/PMDA 等为 Tavily/网络检索，存在内容过期或抓取不全风险（已用 [网络检索] 标签向 LLM 披露，synthesize 亦要求声明"证据有限"）；
3. **LLM 幻觉残余**：虽有不编造 URL 铁律与引用校验，但正文事实仍依赖 LLM 忠于检索结果，无法 100% 消除；
4. **MemorySaver 降级态**：PG 故障时多实例会话不共享、重启丢失（见 3.2#4）。

总体评级：**鲁棒性设计充分**，四层兜底 + 全节点降级 + 错误隔离，关键路径不存在单点故障。

---

## 五、多轮检索中工具选择与检索词拓展的策略

### 5.1 工具选择策略（plan/adjust 节点）

**意图 → 工具映射表**（写死在 `plan.py` / `adjust.py` 的 SYSTEM_PROMPT 中，`get_tool_descriptions()` 动态补充全量清单）：

| 意图 | 首选工具组 |
|---|---|
| 基础信息（drug_info） | pubchem / drugbank / chembl / drugcentral |
| 辅料制剂（excipient_info） | fda_iig / fda_unii / fda_ndc / dailymed |
| 说明书注册（registration） | dailymed / fda_orange / fda_drugs / ema / cde / pmda |
| 安全交互（safety/interaction） | fda_faers / sider / ddinter |
| 分类标准（general/标准） | rxnorm / who_atc / ich |
| 文献专利（literature） | pubmed / espacenet / cnipa |
| 任何场景兜底 | anysearch_fallback_tool（仅在专有源空/不足时启用） |

选择规则：每轮**最多 8 个工具**；优先 [官方API]；自动剔除 `failed_tools` 中已失败的源；`adjust` 轮强制要求**优先选用前几轮未用过的工具**。

### 5.2 差异化查询词策略（plan 生成）

同一问题对不同源生成**不同的查询词**，避免"一份 query 打所有源"的低效：

| 源 | 查询词规范 | 示例 |
|---|---|---|
| PubChem | 纯化合物名（≤3 词），避免多词描述句 | `aspirin`（而非 `aspirin molecular weight`） |
| DailyMed | `药名 + prescribing information` | `aspirin prescribing information` |
| PubMed | `药名 + 主题词`（mechanism of action / adverse effects） | `aspirin mechanism of action` |
| 中文源（CDE/CNIPA） | 使用中文实体名 | `阿司匹林 专利` |
| SIDER/DDInter | 明确"副作用/相互作用"语义 | `warfarin aspirin interaction` |

### 5.3 查询词拓展策略（expand_queries 节点）

当评估为"不足"且未达轮次上限时，`expand_queries` 对查询词做两种拓展：

1. **实体名穷举**：同义词展开，破解"名字变了导致查不到"的死循环 —— 化学名 / 商品名 / 代号（如 G-119）/ 中文名 / CAS 号 / INN；
2. **维度分解**：把原问题按"基本信息 / 机制 / 安全 / 注册 / 制剂"拆成多个独立检索子问题。

### 5.4 调整策略（adjust 节点）

- 以 `missing_info`（evaluate 输出的缺失清单）为输入，逐项补齐；
- 注入 expand 结果与已用工具/已用查询词历史，LLM 明确要求"**换工具 + 换查询角度，不与前几轮重复**"；
- 命中规则兜底条件（失败源≥3、质量分<0.4）时直接启用泛搜。

### 5.5 程序化兜底（decide 节点）

- LLM `is_sufficient=True` 为主判据，但被规则分制衡：质量分 <0.4 或零成功源时**无视 LLM 强制追加一轮**；
- 达 3 轮上限仍不足 → `final_search` 由 LLM 生成 1-3 条新 query 走 AnySearch，重算 `cannot_answer`，合成时明确告知"证据有限，建议核实 FDA/NMPA/PubChem 等官方来源"。

### 5.6 策略小结

系统对"工具选择"采用**意图映射 + 成色标签 + 失败回避 + 换源优先**四重约束；对"检索词"采用**差异化查询 + 同义词穷举 + 维度分解 + 换角度重写**四重手段；辅以**规则分强制兜底**防止 LLM 误判。策略闭环完整，能覆盖"首轮选错工具 / 查询词过窄 / 同义词翻车 / 客观查不到"四类典型失败模式。

---

## 附录 A：关键文件索引

| 文件 | 作用 |
|---|---|
| `backend/agent/graph.py` | 图装配、条件路由、checkpointer（PG 优先/MemorySaver 降级） |
| `backend/agent/state.py` | `AgentState` 定义、messages reducer |
| `backend/agent/nodes/understand.py` | 实体/意图/子问题、闲聊短路、指代消解 |
| `backend/agent/nodes/plan.py` | 工具选择 + 差异化查询词、`_validate_plan` |
| `backend/agent/nodes/retrieve.py` | 并行调用、citations 解析、AnySearch 轮次兜底 |
| `backend/agent/nodes/evaluate.py` | 语义评估 + 规则质量分 + synthesize（铁律） |
| `backend/agent/nodes/decide.py` | 白盒路由、force_fallback、防死循环 |
| `backend/agent/nodes/expand.py` | 查询词扩充（实体穷举 + 维度分解） |
| `backend/agent/nodes/adjust.py` | 缺失信息重规划、换源换词 |
| `backend/agent/nodes/final_search.py` | 末轮泛搜补全、cannot_answer 重算 |
| `backend/tools/__init__.py` | 工具注册表、SOURCE_TYPE 成色标签、描述生成 |
| `backend/tools/engines/` | AnySearch / Tavily / Exa 底层检索引擎 |
| `backend/tools/sources/` | 31 个数据源工具实现 |
| `backend/config.py` | max_retrieval_rounds=3、DeepSeek 模型、DB/Redis 配置 |
| `backend/main.py` | FastAPI + SSE 流式接口、JWT 认证、速查独立入口 |

## 附录 B：尽调总体结论

| 维度 | 评级 | 一句话结论 |
|---|---|---|
| 工具覆盖面 | ★★★★★ | 31 工具覆盖基础信息/辅料/注册/安全/靶点/标准/文献专利/兜底全链路 |
| 整合流程 | ★★★★☆ | 9 节点闭环 + 三条件路由清晰；闲聊短路与防死循环到位 |
| 多轮对话 | ★★★☆☆ | 基础可用的注入式记忆；缺会话压缩与显式记忆管理（最大短板） |
| 系统鲁棒性 | ★★★★☆ | 四层兜底 + 全节点降级 + 错误隔离；残余风险在外部源可用性与 LLM 幻觉 |
| 检索策略 | ★★★★★ | 意图映射 + 差异化词 + 同义词穷举 + 换源换词 + 规则强制兜底，闭环完整 |
