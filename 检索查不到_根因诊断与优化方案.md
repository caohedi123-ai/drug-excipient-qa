# 检索「直接返回找不到」根因诊断与优化方案

> 诊断范围：backend 检索内核（`agent/nodes/*`、`tools/*`、`config.py`、`main.py`）
> 结论：未改动任何业务代码，仅做静态走查 + 注册表核对。

## 一、现象
用户提问后 UI 直接给出"找不到 / 未找到"，没有多轮拆解、没有泛搜兜底，体验上像"配了一堆工具却一点都查不到"。

## 二、关键发现（带位置）

### 发现 1：AnySearch 泛搜从未接入主流程兜底
设计文档描绘"三引擎兜底（专有源 / Tavily / AnySearch）+ Phase 2 泛搜"，但代码里：
- `anysearch_vertical` 只在 2 个源内部被调用——`cde.py`（中国注册）与 `drugcentral.py`（该源降级补充），**从来不是 LLM 可选择的独立工具**。
- `anysearch_engine.py` 里预留的 `anysearch_search(..., "供 fallback_chain 使用")`，**全仓无任何调用方**。

```27:27:backend/tools/sources/cde.py
    return anysearch_vertical(
```
```62:65:backend/tools/sources/drugcentral.py
    # Layer 2: AnySearch 降级（仅当 API 无结果时）
    if not indications:
        any_result = anysearch_vertical(query, domain="health", max_results=5)
```

### 发现 2：工具实际只注册了 12 个，AnySearch 不在内
`PHASE1_TOOLS` 只有 12 个具体数据源工具；`get_tool_descriptions()` 也是硬编码这 12 个。设计所说 24 个工具仅实现 12 个，且泛搜兜底缺位。

```29:29:backend/tools/__init__.py
PHASE1_TOOLS = [
```
```53:58:backend/tools/__init__.py
def get_tool_descriptions() -> str:
    lines = []
    for t in PHASE1_TOOLS:
```

### 发现 3（根因核心）：失败的工具被标记为 `success=True`
各源工具在内部 `try/except` 捕获异常后，返回的是**描述失败的字符串**（如 `[PubChem] 未找到...`、`[DDI] 工具调用失败: ...`），而不是抛异常。
`retrieve.py` 用 `asyncio.gather(return_exceptions=True)` 收集，收不到异常，于是 `raw` 是失败字符串，仍被**硬编码 `success: True`** 塞进 `retrieval_results`。

```80:92:backend/agent/nodes/retrieve.py
            else:
                logger.warning(f"[retrieve] 工具 {tool_name} 返回空结果")
                raw = f"[{tool_name}] 未返回任何结果。"

            # 解析 citations（从返回字符串中的 __citations__: {...}）
            updated_citations = _extract_citations(raw)

            if updated_citations:
                citations.extend(updated_citations)

            all_results.append({
                "content": raw,
                "success": True,   # ← 失败字符串也记为成功
```

### 发现 4：质量评估因此被"虚高"，兜底链不触发
`validate.py` 的 `source_score = min(success_sources/3, 1)` 把假成功算作真成功。一旦有 ≥3 个"成功"源（哪怕内容全是"未找到"），`source_score=1.0`，综合分容易 ≥0.6 阈值 → `evaluate` 判 `is_sufficient=True` → **一轮内直接 synthesize 出"未找到"答案**（这就是"直接返回找不到"的最可能路径）。

```41:42:backend/agent/nodes/validate.py
    success_sources = sum(1 for r in retrieval_results if r.get("success"))
    source_score = min(success_sources / 3.0, 1.0)
```

### 发现 5：`broaden_search` 救不了，只会重 plan 同样的源
`decide.py` 的 `broaden_search` 仅把 `intent` 改成 broad，再走 `plan`——还是从同样的 12 个源里选，同样的失败。没有"对任意查询都跑一次 AnySearch 泛搜"的保底。

### 发现 6：最多 3 轮后强制合成"未找到"
`config.max_retrieval_rounds = 3`。若质量分始终不够，3 轮空转后 `decide` 强制 `synthesize`；按铁律"无法回答就诚实告知"，输出"未找到"。

## 三、一句话根因
**AnySearch 泛搜没有成为主流程的通用兜底工具（仅在 cde/drugcentral 源内使用），且失败源被错误标记为 `success=True`，既没有真实降级链，也没有"对任意查询都跑一次泛搜"的保底。于是 LLM 选中的专有源一旦不可用（API key 缺失 / MCP 未连 / 网络不通），就直接走到"未找到"。**

## 四、优化方案（待确认后实施，本次未改代码）

1. **修复 success 判定**：`retrieve.py` 解析工具返回字符串，识别"未找到 / 调用失败 / error"并标记 `success=False`，让质量评估真实反映失败，触发兜底而非误判充分。
2. **把 AnySearch 注册成独立兜底工具**：在 `TOOLS_BY_NAME` 增加 `anysearch_tool`（不限域名泛搜），让 `plan`/`decide` 能在专有源失败时主动调用。
3. **增加"保底泛搜节点"**：无论前序结果如何，在最后合成前对原始 query 跑一次 AnySearch，保证"配了泛搜就一定用得上"。
4. **建立真实 fallback_chain**：专有源失败 → Tavily 定向 → AnySearch 泛搜，逐级降级（`anysearch_search` 已预留，补调用方即可）。
5. **失败溯源驱动兜底**：`retrieve` 对失败工具记录 `failure_reasons`，`decide` 据此决定 broaden / fallback，而非盲目重 plan。
6. **可观测性日志**：在 retrieve/evaluate/decide 打结构化日志（每轮每个工具的 success / 失败原因），便于定位"这次为什么没查到"。
7. **兜底措辞收敛**：`synthesize` 仅在"零有效结果"时才说未找到；若泛搜有结果必须基于泛搜作答，杜绝"有兜底却说没查到"。

## 五、建议的验证方式（优化后）
- 用一条已知"专有源查不到、但泛搜能查到"的问题（如偏门辅料别名）跑 E2E，确认最终答案来自 AnySearch 而非"未找到"。
- 在 decide/evaluate 日志中确认兜底工具被调用、`success=False` 被正确记录。
